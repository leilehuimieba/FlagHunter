# 结构债总账：全仓审计（Structural Debt Ledger）V1

- 日期：2026-06-23
- 方法：多 agent 并行审计（10 路子系统并行扫描 → 每条候选债独立**证伪式**核验 → 综合分诊）。68 agent / ~6.2M token 两轮（首轮综合被限流打断，扫描+核验缓存后续跑）。
- 规模：**57 条候选 → 证伪剔除 17 条（有意设计/误报）→ 40 条确认真债**。
- 相关：[[FlagHunter_架构决策记录_自顶向下骨架与两关节契约_2026-06-17_V1]]（ADR 含不变量 I2/I3/I5）、[[基准_CTF能力与可达性_2026-06-18_V1]]、[[并行任务卡_backlog]]（D1–D4 已收口）。

## 执行进度（live，逐刀回写）

> 纪律:一次一刀/批、主控亲核该条 file:line、独立 commit、全量 gate 零回归。

- ✅ **第一波 删死代码（7 项,2008 passed）** — `1de75d9` llm 3 方法+ModelConfig 预设/for_model · `f78c4db` ctf recall_shortest/local_summary/ssti break_early · `5d773cb` tui /report 死分支+孤儿 · `f86ca20` tools loader get_tool_info/reload_tools。**卡面翻转拦截**:ledger 误指 `test_get_tool_info_*` 为 loader.get_tool_info 测试,实测 `_tool_env.get_tool_install_info`(名字撞字)→未删。
- ✅ **第二波 D1 ingress_handoff 字段读面接通（最高杠杆,2008 passed）** — `6f32c23`。coordinator.execute 单点写 `dispatcher._ingress_handoff`;回归守护把 verified_flag 结构化-handoff 测试翻成真实 param 路径。
- ✅ **第二波 stream 死参 + solve-loop 死解包** — `generate()` 的 stream 参数(def+docstring,方法体从不用,唯一引用是假 LLM 镜像签名)+ `_run_solve_loop` 解包 capability_primitive/choice 后从不用。
- ⚠ **TypeError 死防御 → 亲核翻转·保留(不删)** — ledger 判 ctf_dispatcher:384/web_server:836/mcp_tools:800 的 `except TypeError(ingress_handoff)` 为死防御。**亲核发现非死**:`test_web_server.py:2629/2734/2846/2958/3094` 与 `test_mcp:1178` 的 mock dispatcher 的 `run()` 不接 ingress_handoff,正靠此回退兜底(调用→mock 抛 TypeError→回退重试无 handoff)。H14 型 load-bearing 测试缝,删它要改 6 个 mock、零生产收益 → **定性保留**。第三次卡面翻转拦截(前两次:get_tool_info 名字撞字、D2 死映射 empirical)。
- ✅ **max_tokens 真相源单源化 + 接通 Settings（裁定 8192,行为变更,74 passed）** — **亲核翻转发现**:`Settings.max_tokens=8192` 当前是死配置——4 处 `ModelConfig(...)` 实例化(main:1007/tui:7127/initializer:345/llm:88 兜底)全只传 temperature 不传 max_tokens,实际运行时永远走 `ModelConfig` 硬编码 **4096**;`Settings.max_tokens` 仅在 `web_server:3975` 回显 `"maxTokens"`,从不进 LLM 调用。修:`config.py:15` 改引 `DEFAULT_MAX_TOKENS`(8192 单一源);三处真实实例化补 `max_tokens=get_settings().max_tokens`(对齐 temperature H15 写法)。运行时 4096→8192,Settings 从死配置→承载。第四次卡面翻转拦截(ledger 仅判"令 ModelConfig 默认=DEFAULT",未察 Settings 本身悬空)。
- ✅ **delegate_task 走 Settings（H15 单源,无回归）** — `tools/delegate_task/__init__.py:67` 裸 `LLM()` → `ModelConfig()` 硬编码默认(temperature 0.7),无视 `get_settings()`。**亲核澄清**:model 两路殊途同归(`DEFAULT_MODEL=os.environ["FLAGHUNTER_MODEL"]`、`Settings.model` 默认亦 = 它;CLI `--model` 因 `update_settings` 全仓零调用本就不写回 Settings → 属预存更大坑不在本刀)。真绕过是 config:子代理恒跑 temperature 0.7 无视配置。修:`LLM(model=settings.model, config=ModelConfig(temperature=settings.temperature, max_tokens=settings.max_tokens))`,对齐 main/tui/initializer 三站点 H15 写法。默认值恰同(0.7)故无可观测变化,但用户改 Settings.temperature 时子代理现跟随。
- ✅ **第三波 kind→chain 单源化 + 修真 bug（jwt 反查,22 passed）** — 两张图漂移:`hypothesis_engine._CHAIN_BY_KIND`(生成期 choose_chain_order)与 `ctf_dispatcher._CHAIN_NAME_FOR_HYPOTHESIS`(解题期反查 _select_hypothesis_for_chain)本是同一 kind→chain 关系的正/反向用法,却各写一份。**亲核核实生成点**:dispatcher 图缺 `jwt_manipulation`(engine:546 真生成假设)→ jwt 是唯一→"jwt"的 kind,反查恒选不中、回退 hypotheses[0]=**真尖锐 bug**;engine 图缺 `contact_report_chain`(registry:716 真生成)但**无害**(engine 末尾恒追加"web",而它→"web")。修:engine 图补 contact_report_chain 成完整 canonical 源;dispatcher 图改为 `= _CHAIN_BY_KIND` 别名(同一对象,27键,以后不可能再漂)。加 jwt 反查回归守护测试。
- ✅ **第三波 原子词表三处 → 亲核翻转·基本误报，仅落守护测试（第五次卡面翻转）** — ledger 记"原子词/链名词表抄三处"。**亲核散成四处、跨两个语义维度,无一处可安全字面合并**:#1 `_CHAIN_BY_KIND.values()`(kind→链 路由目标集,已 canonical)与 #2 `_chain_handler_map` keys(链→执行器 派发键)今日值集**全等(9链)**但绑定方法无法字面合并;#3 `coordinator.py:1518` 卡死检测子集含**死元素 `"ssti"`**(`detect_type()` 输出域 ={misc,upload,lfi,ssrf,xss,sqli,jwt,web},永不产 ssti;`choose_chain_order` 也只 emit `_CHAIN_BY_KIND.values()`+detected+web → chain_name 永不为"ssti")+漏 jwt/ssrf/misc,改它=改 stuck-detection 行为故**不动**;#4 `pa_agent.py:32 _WEBISH_CTF_TYPES`(题型分类 web vs crypto/pwn,用 `cmd` 非 `cmdi`)是**另一维度**不可合并。真正价值=#1⊆#2 的「可路由链必有 handler」隐式不变量(违反即落 robots.txt 兜底,正是引擎注释警告的地雷);两表示无法共享字面,故**加守护测试 `test_chain_handler_map_covers_all_routable_chains` 锁死**。纯测试追加、零生产改动。
- ✅ **第三波 flag 正则单源化（`55ed2cc`,广义本体收敛,52+177 passed）** — 探子穷尽测绘 11+ 处 flag 正则(grep 另补出探子漏掉的 binary/executor/reasoning 三处),**绝大多数因捕获组数/flags(IGNORECASE)/语义范围/内联脚本/用户可配置/测试码不可安全合并**。唯一真·低风险单源点:广义 flag 正则**本体字符串**逐字节复制于三处可动副本——`dispatcher_helpers._FLAG_RE`(1 捕获组)、`verifier._BROAD_FLAG_PATTERN`、`ctf_planner.FLAG_PATTERN`(后两 0 组)。修:`dispatcher_helpers` 抽 canonical `_FLAG_BODY`(0 组),`_FLAG_RE=re.compile(rf"({_FLAG_BODY})")` 派生(pattern 逐字节不变、仍 1 组);verifier/ctf_planner 反向 import 复用本体删字面副本。**安全亲核**:dispatcher_helpers 零顶层包内导入 leaf,反向依赖不可能成环(实测 import 通过);两消费方皆 `.search()` 布尔(verifier 拼 `^(?:...)$`、ctf_planner→submit_success_pattern→`re.search`),0 组本体替换语义不变;`_FLAG_RE.pattern` 与旧字面 1 字节不差(已断言)。两处内联 RCE 脚本(php_exploit_chain/artifact_forensics)同形正则须自包含按设计不动。
- ✅ **第三波 收官** — kind→chain(`cdaf651`)+ 原子词表亲核翻转(`c82d3a3`)+ flag 正则本体(`55ed2cc`)三子项全做。
- ✅ **第四波 `_ctf_dispatcher_hint` 入口层去重下沉（198 passed）** — cli/web/mcp 三处 producer 各约 60 行**字节级近同**的 `decision_parts` 构建块(decisionKind/nextAction/driver/reason + strongestHypothesis 三字段 + probe→endpoint/verify_runtime→runtimeFlag/verify_or_submit→verifiedFlag)抽到 canonical home `control_contract.build_control_decision_parts(decision,snapshot,*,endpoint_fallback="")`。**亲核澄清信封不可合**:输入类型(kwargs / `task:dict` / `TaskEntry`)、base_hint 前缀、resume_context/bootstrap、web 的 `should_include_decision_block` 门控各异——只抽真共享的纯 `(decision,snapshot)` 核,三处各叠自己信封。**唯一核内差异参数化**:mcp 的 probe 分支多一句 `handoff["endpoint"]` 兜底 → 提为 `endpoint_fallback` 参数(cli/web 传默认 `""` 行为不变、mcp 传 handoff endpoint 行为不变);三个 nextAction 分支互斥故核内排序与原各处不同但输出恒同。归宿选 `control_contract`(已含 `strongest_hypothesis_contract`/`_normalize_string_list`、三 interface 均 import、leaf)。cli 删去因此空悬的 `strongest_hypothesis_contract` import(web/mcp 他处仍用故留)。守护:三契约套件全绿 + `test_control_contract` 补两条核直测(全字段链 + endpoint_fallback 仅 mcp 生效)。三处 ~60 行 dup 块收敛为单一核(本刀生产+测试 151 ins/169 del,`e577c9c`)。**注命名**:ledger row 51 拟名 `build_local_ctf_assets_core`,实测只该抽 decision-block 核(非整个 local_ctf_assets 信封),故落名 `build_control_decision_parts` 更贴返回物。
- ✅ **第五波·刀1 web_server 分簇:抽 decision/provenance summary 簇（119 + 2012 passed）** — god-module 物理分文件第一刀。web_server.py 里 13 个纯 `(control_decision, blackboard_snapshot)→summary dict` 的 builder(`_decision_fact_value` + `_build_*_summary` ×11 + `_build_next_action_explanation`/`_build_ingress_handoff_explanation`)整簇搬到新 sibling `web_decision_summaries.py`,web_server 顶部 `from .web_decision_summaries import (...)` re-import 全部回命名空间。**保签名/保导入面**:这 13 个全是 web_server 内部私有(tests 与包内零直接 import;`mcp_tools._build_next_action_explanation` 是同名异参 `entry:TaskEntry` 撞字非共享),调用方(`_serialize_task`/`_task_detail_payload`/route handler)全留 web_server 经其命名空间解析 → **测试 monkeypatch 面**(`emit_log`/`_persist_tasks`/`_pick_metrics_for_task`/`_pick_session_snapshot`/`_build_run_session_context`/`_bus`)**全不受影响**(亲核前置摸清这 7 个 patch 名,确保被搬簇既不在其中、也不调用任何留守 patch 函数→无"from import 重绑陷阱")。依赖仅 `normalize_blackboard_snapshot`(.blackboard_lite)+`strongest_hypothesis_contract`(.control_contract)+typing,两源 leaf 不引 web_server → sibling 无环。web_server 5432→4900 行(`e22af44`)。**方法论**:god-module 机械分簇的正确前置=先全量测绘"测试对本模块的 patch/重绑面",只搬"不被 patch 且不调用留守 patch 函数"的叶簇,callers 留原模块经命名空间解析,patch 语义零变。
- ✅ **第五波·刀2 web_server 分簇:抽 knowledge-doc 解析叶簇（119 + 2012 passed）** — god-module 分文件第二刀。7 个纯 knowledge-document 解析/匹配函数(`_doc_key`/`_decode_doc_key`/`_knowledge_tags_for`/`_chunk_markdown`/`_build_knowledge_doc`/`_doc_match_tokens`/`_message_mentions_doc`,文件内**连续** 101 行)整簇搬到新 sibling `web_knowledge_docs.py`,web_server re-import 回命名空间。**关键判据=向下闭合(down-closed)**:这 7 个互相只调簇内 + stdlib(base64/datetime/Path),**不回调 web_server 任何 helper** → sibling 不引 web_server → 无环。对照:同域的 `_build_knowledge_usage` 因反向调 `_task_session_lookup`/`_iter_all_session_paths`/`_load_json_file`/`_message_time_at`/`_parse_iso`(web_server 通用 helper)**会成环**,故留守 web_server 经命名空间解析调用搬走的两个匹配函数;route handler 亦留守。全 web_server 私有(包内零外引;`_doc_key` 仅 1 处测试引用,re-import 保住),无一在 patch 面。web_server 4900→4806 行(`3464c24`)。**方法论补强**:刀1 的"摸 patch 面"之外,分簇第二判据是"**被搬簇必须向下闭合**"——回调 web_server 通用 helper 的整合器(`_build_knowledge_usage`)会令 sibling 反向 import 成环,只能留守;能搬的是叶子(只调簇内+stdlib/leaf)。
- ✅ **第五波·刀3 web_server 分簇:抽 resume/outcome summary 簇（119 + 2012 passed）** — god-module 分文件第三刀。8 个纯 resume/session-context summary builder(`_normalize_exploit_provenance`/`_exploit_summary_parts`/`_normalize_outcome_action_path_summary`/`_derive_resume_context_from_latest_checkpoint`/`_build_resume_ingress_summary`/`_build_resume_state_summary`/`_build_checkpoint_state_summary`/`_build_runtime_outcome_summary`,文件内**连续** 255 行)整簇搬到新 sibling `web_resume_summaries.py`。**选簇用 AST 调用图自动化**:脚本算模块内调用图→该簇向下闭合(每成员只调簇内+stdlib,零外漏)+自由名仅 `typing.Any`(纯 dict/list 操作无 datetime/json)+tests/包内零外引+无一在 patch 面,四项机器核实后才下刀。web_server 4806→4559 行(`829b387`)。**方法论补强**:分簇选择从手读升级为 **AST 自动化**——`ast.parse`+遍历 `Call` 节点建模块内调用图,一次性筛出全部"叶子/向下闭合簇",再用 free-name 分析定位新模块所需 import,比逐个手读快且不漏。
- ✅ **第五波·刀4 web_server 分簇:抽共享叶子层 web_leaf_utils（119 + 全量门）** — god-module 分文件第四刀,**首个非整块·散点抽取**。10 个全模块通吃的零依赖纯叶子(`_now_iso`/`_parse_iso`/`_sort_time_key`/`_duration_ms_for_task`/`_message_time_at`/`_truncate_text`/`_single_line_preview`/`_normalize_string_list`/`_friendly_tool_name`/`_message_role_for`,**分散在 236–2110 行**非连续)抽到新 sibling `web_leaf_utils.py`。**叶子层先抽(主题簇之前)= 反用刀2"_build_knowledge_usage 会成环"教训**:`_now_iso`(12 caller)/`_parse_iso`(8)/`_normalize_string_list`(7)/`_message_time_at`(6)/`_truncate_text`(5) 是被一堆留守函数+未来主题簇共用的真叶子;若让它们留在 web_server,则任何想搬走、又依赖这些叶子的主题簇(trace-timeline/serialize-task…)都得反向 import web_server→成环。先把叶子沉到独立 leaf 模块,web_server 及后续簇都从它 import → 主动断未来的环。**四项机器核实**:向下闭合(彼此+stdlib)、自由名仅 `Any`/`datetime`/`timezone`、tests/包内零外引、无一在 patch 面。**散点抽取手法**:单进程一次读入→内存按 `end_lineno` 倒序删除(避免移行)→原子写回→重解析校验,规避并发写手。re-import 全集保 `web_server._parse_iso` 等与留守 callers 解析不变。web_server 4559→4482 行(`8c2b0e2`)。**方法论补强**:分簇不必拘泥"整块连续"——叶子可散点抽;且**叶子层应在主题簇之前先抽**,以 leaf 模块为公共下游,后续主题簇(依赖这些叶子)才能干净搬出而不成环。
- ✅ **第五波·刀5 web_server 分簇:抽 trace-timeline 簇 web_trace_timeline（119 + 全量门）** — god-module 分文件第五刀,**刀4 叶子层先抽的兑现**。向下闭合主题簇 4 个 builder(`_build_trace_timeline`/`_build_hint_timeline`/`_build_control_decision_timeline_event`/`_build_control_observation_timeline_events`,分布 1231 + 2037–2360 区段非连续)整簇搬到新 sibling `web_trace_timeline.py`(248 行)。**AST 核实**:`_build_trace_timeline` 只调另 3 个簇内成员,其余 3 个零内部调用→**簇内自闭、无 UP-call**;依赖的 `_now_iso`/`_parse_iso`/`_sort_time_key`/`_duration_ms_for_task`/`_friendly_tool_name` 全是刀4 已沉的叶子→**新模块只 `from .web_leaf_utils import` 而不反向 import web_server,兑现了"叶子先抽断未来环"**。唯一外部 caller `_build_trace_payload` 留守,re-import 保 `web_server._build_trace_timeline` 解析不变;4 个函数均不在 patch 面(tests grep 空)。自由名核出 `event`(2240 lambda 形参)为 false positive。web_server 4483→4261 行(`898bc11`)。
- ✅ **预存坑·运行期 model 覆盖写面接通（用户裁定 A,39+194 passed,`11b523c`）** — `update_settings` 全仓零调用的真根因不是"死代码",是**单源化半截桥**:第三波 H15 把读面(delegate_task/web_server/mcp_tools 读 `settings.model`)收敛到单例,但**写面**(运行期覆盖→单例)一直悬空。**亲核流向**:CLI `--model X` 走参数旁路 `AgentSession.create→build_agent_components(model=X)→LLM(model=X)`,只到**主 agent**;`settings.model`(=`DEFAULT_MODEL`=env `FLAGHUNTER_MODEL`)从生不变,故 `delegate_task:74` 子代理仍跑 env model→**split-brain**(这是 max_tokens 卡面翻转的反向镜像:那个是单例字段没人读=死配置,这个是 3 处读但没人写=写面悬空)。修:(1)`update_settings` full-replace→**就地字段合并**(保留单例身份+其他字段,未知字段抛 `AttributeError`;整体替换会把 target/scope/api keys 打回默认无法承接单字段覆盖);(2)`initializer.build_agent_components`(单一装配路径 I2)model 解析后 `update_settings(model=model)` 发布到单例→三读取点与主 LLM 收敛同源。**行为变更**:`--model` 现在也作用子代理。测试:2 条 full-replace 断言→3 条合并契约 + 1 条装配守护(源码级锁"解析 model 写回单例",真实重装配跑不起单测)。**接续**:MCP `update_config` 见下条已闭合。
- ✅ **预存坑续·MCP update_config 接通 model 写面（4 条新测,`94e4ff7`）** — 承接 11b523c,闭合 model 写面**最后一处绕单例入口**。`update_config`(`mcp_tools:1026`)此前只直改 `_primary_agent.target/scope/max_iterations`、不支持 model。**亲核分流**:`settings.target/scope/max_iterations` **全仓零读取面**(grep 空)→ 它们的 live 路径纯是 agent 属性,写单例反成**死配置**(债池正在清的反模式),故保持 agent-direct;唯 **model 有单例消费方**(`delegate_task:74` 子代理读 `get_settings().model`)→需**双写**:(1)`update_settings(model=)` 发布单例→子代理/web 收敛;(2)主 agent live LLM 经 `LLM.set_model()` 改 `self.model`(主循环每调用 live 读 `self.llm.model`,llm.py:320)。`get_config` 加 active model 回显(读面对齐,round-trip 可观测)。守护:schema 含 model + model 双写 round-trip(单例+live LLM 双断言)+ target-only 不触单例 model,共 4 条,mcp 套件 39 passed。**至此 model 写面两入口(CLI initializer / MCP update_config)全部经单例,split-brain 闭合。**
- ✅ **第五波·刀6 web_server 分簇:抽 serialize-task 簇 web_serialize_task（119 + 197 passed,`5d43b5f`）** — god-module 分文件第六刀,**首个全连续块主题簇**。`_serialize_task` 的 callee 闭包 6 函数(连续块 245–356:`_task_capabilities`/`_task_detail_defaults`/`_normalize_task_collections`/`_serialize_task`/`_normalized_blackboard_snapshot`/`_merge_blackboard_snapshots`)整簇搬到新 sibling `web_serialize_task.py`(140 行)。**AST 四项机器核实**:(1)向下闭合——6 函数 callee 闭包自封,彼此外不调任何 web_server 模块函数;(2)自由名零 `WEBSERVER-FUNC`——依赖全落已抽出的 sibling/leaf(`.web_decision_summaries` 刀1 的 `_build_*_summary`/`_build_next_action_explanation`、`.web_leaf_utils` 刀4 的 `_duration_ms_for_task`/`_normalize_string_list`、`.blackboard_lite`)→新模块零反向 import web_server 无环,**再次兑现"叶子+前序簇先抽断未来环"**;(3)patch 面——6 个全是测试经 `web_server.` 命名空间直调(`_task_capabilities`/`_serialize_task`/`_task_detail_defaults`)非 monkeypatch→re-import 保解析不变,`mcp_tools._normalized_blackboard_snapshot` 系同名异源撞名;(4)留守 7 callers 经 re-import 命名空间解析不变。**附带死 import 清理**:摘除搬簇后悬空的 `normalize_blackboard_snapshot`(原仅被移走的 `_normalized_blackboard_snapshot` 包,0 load-use,无外部经 web_server 引用)。web_server 4261→4155 行。**方法论**:连续块簇比刀4/5 散点抽更简单,但前置核实(AST 闭包+自由名+patch 面)不变;依赖落在前序刀已抽出的 sibling 上,坐实"逐刀沉淀的公共下游让后续主题簇干净搬出"。
- ✅ **第五波·刀7 web_server 分簇:抽 detail-section 簇 web_detail_sections（119 passed,`2bc422f`）** — god-module 分文件第七刀,**方法论扩展:被 patch 函数亦可搬**。`_task_detail_payload` 的明细段 builder 3 函数(连续块 1005–1113:`_build_messages_from_snapshot`/`_build_plan_from_snapshot`/`_build_messages_from_metrics`)整簇搬到新 sibling `web_detail_sections.py`(131 行)。**AST 核实**:向下闭合(module-callees 全空)+ 自由名仅 typing.Any+web_leaf_utils 4 叶子→零反向 import 无环。**patch 面关键判断(扩展前序刀1"不搬 patch 函数"的保守规则)**:这 3 个**全被测试 monkeypatch**(`test_web_server:5465/5472/5560`),按刀1 字面规则本应不搬;但亲核 re-bind 陷阱机理——陷阱只在「caller 也在被搬 sibling 里(解析走 sibling 全局、看不见 web_server 命名空间的 patch)」时发作。这里唯一 caller `_task_detail_payload` **留守 web_server**、测试 patch 的是 **`web_server.` 命名空间**,caller 经 web_server 全局解析→见 patch;且无 sibling 内 caller。故安全。**坐实新不变量**:被 patch 函数可搬,充要条件=「全部 caller 留守 web_server ∧ 测试 patch 走 web_server 命名空间」(刀1 的"不被 patch"是充分非必要);119 专项含这些 monkeypatch 用例全绿即证。附带摘除悬空 `_message_role_for` leaf import。web_server 4155→4049 行。
- ✅ **第五波·刀8 web_server 分簇:抽 knowledge-hits 簇 web_knowledge_hits（119 + 全量门,`1dedf20`）** — god-module 分文件第八刀,**比 刀6/刀7 更干净(无死 import 要摘)**。knowledge 检索时间线重建簇 4 函数 + 簇内常量(连续块 1025–1154:`_KNOWLEDGE_TOOLS`/`_knowledge_result_kind`/`_parse_knowledge_score`/`_build_knowledge_hits_from_snapshot`/`_build_knowledge_hits_from_metrics`)整簇搬到新 sibling `web_knowledge_hits.py`。**亲核 `_KNOWLEDGE_TOOLS` 仅本簇 3 处用→随簇搬**(不留 web_server)。**AST 四项机器核实**:(1)向下闭合——`_knowledge_result_kind`/`_parse_knowledge_score` 仅簇内调,2 个 builder 仅留守 `_task_detail_payload`(L1534/1537)调;(2)自由名零 WEBSERVER-FUNC——依赖全落 web_leaf_utils 叶子(`_message_time_at`/`_truncate_text`/`_now_iso`/`_single_line_preview`)+stdlib `re`+簇内常量→零反向 import 无环;(3)patch 面——4 函数 + 常量**均不在测试 patch 面**(earlier grep 未列;源码层零外部引用,`.pyc` 命中系陈旧字节码);(4)留守唯一 caller `_task_detail_payload` 经 re-import 解析不变。**无死 import**:搬簇后 `re`(另 6 处用)/`_single_line_preview`/`_now_iso`/`_message_time_at`/`_truncate_text`(均留守在用)无一变死,比刀6(摘 normalize_blackboard_snapshot)/刀7(摘 _message_role_for)更省。簇内 helper `_knowledge_result_kind`/`_parse_knowledge_score`(无留守 caller)按既定模式全员 re-import 保命名空间稳定。web_server 4049→3917 行(−132)。**方法论**:坐实"逐刀沉淀公共下游(leaf+前序簇)让后续主题簇干净搬出";本刀连续块 + 零 patch 面 + 零死 import = 分簇范式最干净一例。
- ✅ **第五波·刀9 web_server 分簇:抽 settings/env-IO 簇 web_settings_io（119 + settings/mcp 81 + 全量门,`7df4e14`）** — god-module 分文件第九刀,**最自包含一例:零 web_server 依赖(连 leaf 层都不依赖)**。`.env` 读写 + UI settings-payload 投影簇(**连续块** L2276–2564,7 函数 `_read_env`/`_write_env_key`/`_mask_secret`/`_settings_meta`/`_mcp_manager_for_project`/`_settings_to_api`/`_apply_settings` + 簇内 2 常量 `_SETTINGS_EDITABLE_PATHS`/`_SETTINGS_RESTART_REQUIRED_PATHS`)整簇搬到新 sibling `web_settings_io.py`(307 行)。**AST 四项机器核实**:(1)向下闭合——成员只调彼此(`_settings_to_api`→`_read_env`/`_mcp_manager_for_project`/`_mask_secret`/`_settings_meta`;`_apply_settings`→`_read_env`/`_write_env_key`),零 web_server up-call;(2)自由名——仅 `Path`(签名注解)/`Any`(返回注解)+全部跨模块依赖(`get_settings`/`resolve_model_provider`/`resolve_model_readiness`/`AGENT_MAX_ITERATIONS`/`MCPManager`)都是**函数内 lazy import**→新模块只 `from pathlib import Path`+`from typing import Any`,**连 web_leaf_utils 都不依赖**(史上最自包含簇);(3)patch 面——7 函数+2 常量经 grep 测试**零 `web_server.X` 引用**、包内零外引;(4)留守 caller 全在 `_make_handlers` 的 route handler(get/put settings)经 re-import 命名空间解析不变。2 常量仅簇内 4 处用→随簇搬。web_server 3917→3642 行(−275)。**附带亲核发现既有死 import**(非本刀致死):`base64`/`os`/`time`(L11/14/17)在 web_server 与本簇均零使用,极可能 刀2 搬 knowledge-doc 解析(用 base64)时遗留→**记为独立微清理候选,不捆绑本刀**(每刀只清自己搬簇致死的 import,守纯抽取语义)。**方法论**:连续块 + 全自包含(lazy import 让跨包依赖不上浮为 module-level)+零 patch 面 = 分簇最低风险形态;识别"lazy import 簇"可作优先抽取信号。
- ✅ **第五波·刀10 web_server 分簇:下沉共享叶子 _workspace_name_for_target/_load_json_file → web_leaf_utils（168 专项 + 全量门,`d86c8e4`）** — god-module 分文件第十刀,**第二次叶子层下沉(同刀4 精神),为 刀11 抽 notes 簇扫前置**。亲核 notes 簇候选时发现 `_workspace_name_for_target`(L861,仅 `re.sub`)与 `_load_json_file`(L871,仅 `json.loads`+`read_text`)是**纯叶子且被两簇共用**:notes 簇(`_candidate_notes_files`/`_load_task_notes`)+ session-IO 簇(`_iter_session_paths` L943 / session lookup L1003/1006/1014/1817)。**关键判断**:随任一主题簇搬都会让**另一簇反向 import** 该 sibling→成环;按"叶子先抽断未来环"必须**下沉 web_leaf_utils**(非随簇搬)。**机器核实**:(1)两者纯 stdlib 叶子,向下闭合;(2)tests grep 零 monkeypatch(零 patch 面);(3)web_server re-import 回原命名空间,6 处留守 caller(L880/943/1003/1006/1014/1817)经 web_server 全局解析不变,identity 校验 `web_server._workspace_name_for_target is web_leaf_utils._workspace_name_for_target`=True;(4)删两函数后 `json.`/`re.` 在 web_server 仍各有用(共 18 处)→**未 strand 死 import,本刀零摘除**。web_leaf_utils 补 `import json`/`re`/`from pathlib import Path`,叶子集 10→12。web_server 3642→3625 行(−17),web_leaf_utils 95→114。**方法论**:抽主题簇前先做"跨簇共用扫描"——被≥2 主题簇共用的叶子一律下沉公共 leaf 层,不归属任一簇;坐实"叶子先抽断未来环"在第二轮(notes/session 双簇)同样成立。
- ⏳ **待办（按推荐序列）**:第五波续刀(web_server 现 3625 行,已抽 9 sibling + web_leaf_utils 叶子集已扩至 12;**下一刀11=notes 簇** `_candidate_notes_files`/`_note_matches_task`/`_load_task_notes`,caller `_task_detail_payload`(L1419)留守,刀10 后已向下闭合到 web_leaf_utils,落地前核 patch 面;余候选主题簇见下;FlagHunterTUI ~10337 行待分)**注 #4 既有死 import**:`base64`/`os`/`time`(web_server L11/14/17,刀9 亲核发现,疑 刀2 遗留)零使用,可作后续微清理一刀(独立提交,与分簇解耦)。**注 session-IO 簇被 patch 面污染**:其 callee 闭包含 `_pick_metrics_for_task`(测试 monkeypatch 目标)且 `_task_session_lookup` 反调它→搬它触 re-bind 陷阱(`_task_session_lookup` 若也搬则它对 `_pick_metrics_for_task` 的解析走 sibling 全局看不见 patch);除非把 `_pick_metrics_for_task` 留守(簇就破),否则不安全,**暂缓**——保签名物理分文件,逐簇一刀+全量门,先摸 patch 面 + AST 验向下闭合。**注 #3 死元素 `"ssti"`**(coordinator:1518)可作后续微清理(去 ssti 不变行为,但 jwt/ssrf/misc 是否应纳入 stuck-detection 属意图待定,暂留)。

## 0. 主控亲核背书（不是照抄 agent 报告）

这份总账经主控亲核抽样校验,**抽查的高杠杆结论全部成立、无"卡面翻转"**：
- **D1 ingress_handoff 入口丢参**（rank 1）：✅ 真。`ctf_dispatcher.py:331` 只把 `self._ingress_handoff` 初始化为 None，**全仓无任何处从 `run()` 的 `ingress_handoff` 形参写回该字段** → 字段读面（`_strategy_context:1752`、`:390/394`）恒空。**Nuance**：`_record_ingress_handoff_observations(ingress_handoff)` 走 execute 形参直传、不读字段，那条 observation 记录路径是活的——所以是"字段读面死、observation 路径活"，修法（run()/bootstrap 补一行写字段）正确。
- **kind→chain 双图漂移**（rank 6）：✅ 真。`ctf_dispatcher.py:211 _CHAIN_NAME_FOR_HYPOTHESIS` 与 `hypothesis_engine.py:11 _CHAIN_BY_KIND` 是近重复两图且已漂移——前者**缺 jwt_manipulation/graphql/nosql**，致 `_select_hypothesis_for_chain` 对 jwt 链选不中假设、落默认，污染按 hypothesis_id 聚合的实验反馈。
- **delete 抽查 `_build_local_challenge_summary_from_run`**（rank 11）：✅ 真零消费者（含 tests 全仓仅定义处）。
- **max_tokens 真相源发散**（rank 5）：✅ 真。`settings.py:25 = DEFAULT_MAX_TOKENS` vs `llm/config.py:15/48 = 4096` 两套独立默认。

> 证伪相确实起作用（剔了 57 中的 17 条），故存活项可信度高于以往单跑 agent。但下面每条仍按项目铁律：**落刀前主控亲核该条 file:line + 全量 gate 零回归**，不照单全收。

---

## FlagHunter 结构债总账 + 重构/删除/重写分诊

### 一句话总判断

**这是一座"表层债为主、零地基级原始设计错误"的健康仓库。** 31 条已证伪确认债里，没有任何一条触及引擎选型/分层骨架/核心抽象的原始设计错误（地基级 = 0）；绝大多数是增量重构留下的**收尾尾巴**：被泛化版取代未清理的旧 API、抽函数时漏合并的双写、入口层错置的域逻辑、以及 H15 温度治理漏掉的兄弟配置（max_tokens）。在 **2019 个测试**这张安全网下，**明确建议：增量重构 + 局部缝后重写，绝不推倒重写**。两个 god-module（web_server 5485 行 / FlagHunterTUI ~7800 行）虽体量吓人，但都是模块内私有纯函数聚集，已有逐函数 import 的测试锁定，正确姿势是"测试网保护下按职责机械抽簇"，而非大爆炸。

分诊比例：**就地重构 21 条（68%）/ 删除 7 条（23%）/ 缝后重写 1 条（3%）/ 保留加注 4 条（13% 含重叠口径取整）**。重写仅 web_server 一条，且本质是"保持签名的物理分文件"，非语义重写。

---

### 一、refactor-in-place（就地重构，21 条）—— 主战场

| 标题 | 子系统 | severity | file:line | 第一刀 |
|---|---|---|---|---|
| ingress_handoff 入口被丢弃，structured-handoff 读面恒读到 None | ctf-engine-core | high | ctf_dispatcher.py:349 / coordinator.py:1784 | run()/bootstrap 补 `self._ingress_handoff = ingress_handoff` 一行 |
| _ctf_dispatcher_hint 在 cli/web/mcp 三处复制，producer 无单源 | interface-cli-web | high | cli.py:58 / web_server.py:698 / mcp_tools.py:666 | 抽 build_local_ctf_assets_core(decision,snapshot)，三处调用各叠信封 |
| FlagHunterTUI ~7800 行 god-object | interface-tui | high | tui.py:2616 | 先抽 d3/d6 无头簇缩体积，分阶段 |
| kind→chain 双真相源漂移（jwt_manipulation 缺键） | ctf-engine-core | medium | ctf_dispatcher.py:211 / hypothesis_engine.py:11 | 收敛为单一 KIND_TO_CHAIN，_select_hypothesis 复用反向索引 |
| strategy-priority 在 _select_primary_strategy 与 web_strategy_order 双写 | ctf-engine-core | medium | ctf_dispatcher.py:812 / chains/web.py:71 | 抽共享"按 handoff 推导 web 策略优先级"函数（建议先修 d1） |
| 原子事实词表三处独立硬编码，已漂移导致交集静默失效 | ctf-intelligence | medium | hypothesis_engine.py:845/895 / strategy_memory.py:642 | 抽 atomic-facts 词表/构造模块，三处共用 |
| build_fingerprint 在读路径偷偷写回 observation.metadata | ctf-intelligence | medium | strategy_memory.py:594 | _derive_web_subtype 派生提成显式步骤，构造器回归纯函数（同步改守护测试:451） |
| TUI 平台自治循环在 /ctf 与 /ctf-crew 近 300 行重复 | ctf-platform-crew | medium | tui.py:7529/7823 / ctf_crew_runner.py:14 | 抽无头自治 helper，每题求解器回调注入 |
| CTF flag 核心正则 4+ 处硬编码 | ctf-platform-crew | medium | verifier.py:15 / ctf_planner.py:11 / dispatcher_helpers.py:82 | 抽核心字符类常量，3 个可 import 副本引用；脚本字面量加注释 |
| _auto_generate_plan 生产 loop 从不调用，文档仍宣称强制规划 | agentic-loop-crew | medium | base_agent.py:946 / pa_agent.py:753 / AGENTS.md:288 | 删 1053 死 return + 改 AGENTS.md + 裁决凭据 priming 逻辑归位 |
| orchestrator.run 与 base_agent loop 各写一份 tool_call 解析 | agentic-loop-crew | medium | orchestrator.py:192 / base_agent.py:721 | 抽自由函数 parse util（get_tc_name/args/id ↔ _parse_arguments）|
| CTF 派发 cli/web 分叉：web 建 handoff/snapshot，cli 全不做 | interface-cli-web | medium | cli.py:626 / web_server.py:3519 | 抽 control_decision→handoff→snapshot 共享 helper 供三入口 |
| _run_ctf_dispatcher_with_handoff TypeError 探测形参（死防御）| interface-cli-web | medium | web_server.py:817 / ctf_dispatcher.py:349 | 去 TypeError 回退直接调 run()，连 mcp_tools.py:798 + inner 三处同步 |
| KnowledgeIndexer 与 RAGEngine.index 重复切块，且 Indexer 生产无人用 | session-knowledge | medium | indexer.py:69 / rag.py:120 | RAGEngine.index 的文件→Document 段委托 Indexer；勿搬持久化 |
| delegate_task 裸 LLM() 绕过 Settings 单例与 DEFAULT_MODEL 守卫 | llm-config | medium | delegate_task/__init__.py:67 | 经 get_settings() 取 model+temperature，补进 constants 守卫枚举 |
| max_tokens 真相源发散：Settings=8192 vs ModelConfig=4096，实际用 4096 | llm-config | medium | constants.py:75 / config.py:15 / llm.py:461 | 先裁定权威值，再令 ModelConfig 默认=DEFAULT_MAX_TOKENS（H15 漏的兄弟）|
| Tool.required_permission 死配置，真实权限走 _TOOL_RISK_MAP | tools-mcp-runtime | medium | registry.py:44 / permission_enforcer.py:213 | 收敛单源：delegate_task 的 RECON_ONLY 并入 _TOOL_RISK_MAP 后删字段 |
| _extract_forms_from_html 在 ssh/docker_runtime 整段逐字复制 | tools-mcp-runtime | medium | ssh_runtime.py:23 / docker_runtime.py:534 | 上移纯函数到 runtime.py；_proxy_http_fetch 抽 execute_command 回调版 |
| ~14 个 _render_last_ctf_* 是纯 state→str 域逻辑被 TUI 私吞 | interface-tui | medium | tui.py:8572/8613/9325 | 移到 agents/pa_agent 无头 presenter，TUI 委托；_render_last_ctf_memory 另处理 |
| solve loop 解包 capability_primitive/choice 后从不用 | ctf-engine-core | low | ctf_dispatcher.py:486-487 | 删两行局部解包（coordinator dict 键保留）|
| run() ingress_handoff TypeError 兼容 fallback 无消费者 | ctf-engine-core | low | ctf_dispatcher.py:378 / coordinator.py:1739 | 删 try/except 直接 await execute()（随 d1 一并修）|
| _normalize_string_list cli/web 各一份且语义分叉（去重vs不去重）| interface-cli-web | low | cli.py:43 / web_server.py:253 | 下沉到 control_contract.py:11 去重版，mcp_tools.py:249 一并纳入 |
| generate() 的 stream 参数是死参数 | llm-config | low | llm.py:420/431 | 删参数+docstring 行（H16 漏网尾巴）|
| spawn_agent worker_type 白名单 tools.py 与 worker_pool 双写 | agentic-loop-crew | low | tools.py:26 / worker_pool.py:41 | enum 从 _SKILL_TOOLS.keys() 派生，删 tools.py:28 内联守卫 |
| WorkerPool 与 SubagentRunner 内联猴补 get_system_prompt | agentic-loop-crew | low | worker_pool.py:187 / subagent.py:135 | FlagHunterAgent 加 role_suffix/extra_system_prompt 形参，两处传参替猴补 |
| web_subtype 派生 strategy_memory 与 hypothesis_engine 各一份 | ctf-intelligence | low | strategy_memory.py:975 / hypothesis_engine.py:1193 | 引共享 subtype 名集合常量，三个生产端+读端统一引用 |
| _read_ctf_source_context 源码摄取放在 TUI 入口层 | interface-tui | low | tui.py:4592/4550 | 两函数+三常量机械下沉到 pa_agent 域模块，TUI 改调（随 d3）|
| RAGEngine.index god-method + notify 容错样板 3 处重复 | session-knowledge | low | rag.py:65/103/200 | 抽 _try_load/_try_persist + _warn helper，index() 只编排 |

> 注：上表列了 28 项；ctf_dispatcher 系两条 low 与 d1 联动，实计入 refactor 21 条（含被合并表述）。

---

### 二、delete（删除，7 条）—— 零生产消费者、非测试缝

| 标题 | 子系统 | severity | file:line |
|---|---|---|---|
| recall_shortest_winning_chain 被 recall_chain_pheromone 取代 | ctf-intelligence | low | strategy_memory.py:106 + 2 守护测试 |
| _build_local_challenge_summary_from_run 零消费者 + 重复 | ctf-intelligence | medium | context_assembler.py:200 |
| /report 命令死分支被前面 startswith 遮蔽 | interface-tui | medium | tui.py:5123 + 孤儿 _run_report_generation:4807 |
| ssti break_early 赋值后不读 + 缺 break | ctf-executors | low | ssti_executor.py:512-515 |
| LLM 三方法 simple_completion/get_available_models/update_config 零消费 | llm-config | low | llm.py:589/613/662 |
| ModelConfig 预设 + for_model 仅测试消费 | llm-config | low | config.py:45-62 + test_config.py（协同删）|
| loader.reload_tools / get_tool_info 死代码 | tools-mcp-runtime | low | loader.py:238/260 + __init__ 导出 |

> 删除纪律（H14 教训）：含守护测试的（recall_shortest / ModelConfig 预设 / 三方法）必须**协同删测试**，否则 test 崩。

---

### 三、rewrite-behind-seam（缝后重写，1 条）

| 标题 | 子系统 | severity | file:line | 注意 |
|---|---|---|---|---|
| web_server.py 5485 行 god-module（EventBus/聚合器/任务表/CTF派发/设置/HTTP）| interface-cli-web | high | web_server.py:1-5485 | "缝后重写"=测试网保护下按职责物理分文件、**保持函数签名与导入面**，分批迁移；test_web_server.py 7235 行逐函数 import，落地必须保导入路径或同步改测试 |

---

### 四、keep-with-note（保留加注，4 条）—— 非债或不相称

| 标题 | 子系统 | severity | file:line | 处置 |
|---|---|---|---|---|
| decay 维护在每次读查询里隐式回写记忆文件 | ctf-intelligence | low | strategy_memory.py:841 | 补注释"read 触发 decay 落盘系有意持久化、非并发安全"+守护测试 |
| phase05_baseline 证据注册表放在运行时包 pa_agent/ 下 | ctf-platform-crew | low | phase05_baseline.py:1-234 | 加模块级"评测证据非运行时逻辑"注释，留待统一 benchmarks 迁 |
| 会话持久化三套并存（SessionStore/ConversationStore/session.json）| session-knowledge | medium | session_store.py:59 / conversation_store.py:31 | 用途不同非纯冗余，画职责对照表；点名 SessionStore 无参构造落 cwd 全局 loot 而忽视 workspace 隔离 |
| runtime copy_to_container 靠 hasattr 鸭子判断缺显式契约 | tools-mcp-runtime | low | sqlmap/__init__.py:86 等 5 处 + hybrid_runtime.py:85 | 仿 H18 BrowserCapabilities 加 supports_file_copy()，暂不动以免无收益翻动 5 工具+3 runtime |

---

### 推荐执行序列（一次一刀、可独立 commit、零回归）

**第一波 — 高 ROI 零风险删除（先清死代码，缩小后续噪声）**
1. **D-ssti**：删 ssti_executor.py:512-515 四行 break_early（零行为变化）
2. **D-solveloop**：删 ctf_dispatcher.py:486-487 两行死解包
3. **D-loader**：删 loader.reload_tools/get_tool_info + __init__ 导出
4. **D-llm3**：删 LLM simple_completion/get_available_models/update_config（已核 tests 零引用）
5. **D-report**：删 tui.py:5123 死分支 + 孤儿 _run_report_generation
6. **D-recall / D-modelconfig**：协同删方法 + 守护测试
7. **D-context-summary**：删 _build_local_challenge_summary_from_run

**第二波 — 高 ROI 一行级修复（功能+结构双收）**
8. **d1 ingress_handoff**：run()/bootstrap 补一行写 self._ingress_handoff —— 这一刀同时点亮 d2/d3 的 structured 分支，是最高杠杆点
9. **d5/web-d3 死防御收口**：连同 mcp_tools.py:798 + inner 删三处 TypeError 探测
10. **llm max_tokens / stream / delegate_task**：先裁权威值，再对齐 ModelConfig；删 stream 死参；delegate_task 走 get_settings()

**第三波 — 配置单源化（抽常量，2019 测试兜底）**
11. flag 正则单源（d2 platform）→ kind→chain 单源 → 原子事实词表单源 → worker_type enum 派生 → _normalize_string_list 下沉

**第四波 — 入口层去重/下沉（中等面，分批）**
12. _ctf_dispatcher_hint 三入口抽核心 → cli/web CTF 派发编排 helper → _extract_forms_from_html 上移
13. _render_last_ctf_* / _read_ctf_source_context 下沉无头 presenter

**第五波 — god-module 分簇（长尾，最后做）**
14. web_server 按职责切 web/ 子包（保导入面）；TUI 随 d3/d6 抽簇缩体积

---

### 地基级（foundation_level）债：**0 条**

**没有任何一条触及原始设计错误。** 两个引擎（agentic loop / 确定性 CTF 链路求解机）的选型与分层骨架健康；god-object 是增量增长的体量问题而非错误抽象；所有 config-divergence 都是"抽取/治理半途"留下的第二真相源，而非设计时就规划了两套。最接近"系统性"的是 **ingress_handoff 入口丢参（d1）**——它让整条 structured-handoff 读面运行期恒空、并连带 d2/d3/web-d4 多条债的 structured 分支变死分支，是**最高杠杆的单点修复**，但本质仍是一行写面缺失，非地基设计错误。结论维持：增量优先，先修 d1。