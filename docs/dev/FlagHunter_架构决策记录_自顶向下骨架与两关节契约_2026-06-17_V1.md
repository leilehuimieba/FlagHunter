# 架构决策记录(ADR):自顶向下骨架与两关节契约 V1

- 日期:2026-06-17
- 状态:**已确认 / P0 完成**(零代码改动;选型见 §7)
- 起因:历史优化多为「叶子打补丁」(在 `ctf_dispatcher` 方法或某条 web 策略上补能力),层与层之间各修各的、接不回上层。本 ADR 改为**自顶向下**:先把骨架与层间契约定死,再逐层下沉优化。

---

## 1. 现状诊断(三路静态审计结论)

| 维度 | 结论 | 性质 |
|---|---|---|
| 层间依赖方向 | 严格向下、无循环;foundation(`runtime/llm/config/knowledge/session`)与 capability(`tools/cpa_modules`)**无上向依赖**。`llm/llm.py` 对 `cpa_modules.m1_api_hub` 的引用是函数内动态 import,不构成模块级环。 | **地基健康** |
| 关节 A(入口→编排) | 4 入口仅在 `BaseAgent.agent_loop()` 类层统一;编排/事件/结果生命周期**全碎片**。已有共享初始化器 `build_agent_components()`,但**只有 TUI / main.py 用,CLI / web_server 绕过** → cpa_modules m1–m6 被静默跳过(correctness bug,行为不一致)。 | **缺契约 + 1 bug** |
| 关节 B(编排→策略) | `_execute_chain` 用硬 if/elif 分发而非 registry;40 个 chain 方法约 **60% 硬编码 / 40% 在 registry**;chain 全靠 `self.state / self.strategy_memory / self.runtime` 上帝对象隐式取状态,无显式传参。 | **缺契约 + 上帝对象** |

**核心判断**:骨架地基(依赖方向)优秀,缺的是两个**承重关节没浇筑契约**。本 ADR 只定这两个契约 + 落地顺序。

---

## 2. 目标骨架

```
ENTRY(薄)      TUI   CLI   web   MCP          只做 I/O + 渲染;不直接 new LLM/Tools/Runtime
                         │  仅依赖关节 A 契约
SESSION ★A     AgentSession 门面               关节 A:唯一装配入口 + 单一事件总线 + 结果/会话生命周期
                         │  agent_loop 契约
ORCHESTRATION  BaseAgent.agent_loop()          契约已在,P2 仅校验 + 薄适配
                         │  关节 B 契约
STRATEGY ★B    StrategyRegistry 单一分发        关节 B:registry 驱动取代 if/elif
               chains/{web,sqli,xss,ssti,...}   ChainContext 显式传状态,拆上帝对象
                         │
CAPABILITY     tools/   cpa_modules/m1..m6      方向已干净,仅命名/文档
FOUNDATION     runtime llm knowledge config     无环
```

不变量(Architecture invariants,任何阶段不得破坏):
- **I1 依赖单向向下**:foundation 不得 import capability/orchestration/entry;capability 不得 import orchestration/entry。CI/审计可用 grep 守。
- **I2 唯一装配入口**:任何入口构造 agent 必须经 `build_agent_components()`,不得自己 new LLM/Tools/Runtime。
- **I3 事件单源**:agent 运行产生的事件只走一条总线(关节 A 的 EventBus),入口只订阅、不自建并行机制。
- **I4 chain 不读 self 上帝对象**:策略/chain 通过 `ChainContext` 取所需状态,禁止新增对 `dispatcher.*` 的隐式访问(存量逐步收敛)。

---

## 3. 关节 A 契约:`AgentSession`(入口 ↔ 编排)

### 3.1 现状落点(真实代码)
- 共享装配器:`pentestagent/interface/initializer.py:167` `async def build_agent_components(...) -> dict[str, Any]`,返回 `{agent, runtime, runtime_info, rag_engine, ...}`,内部按标准顺序初始化并触发 cpa_modules m1–m6 钩子(`initializer.py:252-311`)。
- 运行契约:`pentestagent/agents/base_agent.py:262` `async def agent_loop(initial_message: str) -> AsyncIterator[AgentMessage]`。
- 现有碎片化事件路径:TUI 用 `interface/notifier.py` 回调;web 用私有 `EventBus`(`web_server.py`);CLI 用 `rich` 直输;MCP 用全局 `_emit` + `_ui_hook`。

### 3.2 目标契约(P0 仅定义,P1 落地)
新增 `pentestagent/session/agent_session.py`,作为 4 入口与编排层之间的**唯一门面**:

```python
class AgentSession:
    """入口与 agent 引擎之间的唯一门面。封装装配、运行、事件、结果生命周期。"""

    @classmethod
    async def create(cls, *, target=None, scope=None, model=None,
                     docker=False, ssh=False, no_rag=False, no_mcp=False,
                     on_progress=None) -> "AgentSession":
        """唯一装配路径 —— 内部必须调用 build_agent_components(...)。
        消除 CLI/web 跳过 cpa 初始化的 bug(满足 I2)。"""

    def events(self) -> "EventBus":
        """单一事件总线;入口只订阅这里(满足 I3)。事件为结构化 dict/dataclass,
        统一替代 notifier 回调 / web 私有 EventBus / MCP _emit 三套。"""

    async def run(self, task: str) -> "RunResult":
        """驱动 agent_loop(task);逐条 AgentMessage 转成事件 emit 到 events();
        汇总为 RunResult{flag, findings, tokens, status, session_id}。"""

    def session_store(self) -> "SessionStore": ...
    def metrics(self) -> "MetricsCollector": ...
```

入口改造后职责收敛为:`session = await AgentSession.create(...)` → 订阅 `session.events()` → `await session.run(task)` → 渲染 `RunResult`。**TUI/CLI/web/MCP 不再各自 new LLM/Tools/Runtime,不再各搞事件机制。**

### 3.3 关节 A「完成定义(DoD)」
- 4 入口全部经 `AgentSession.create` → `build_agent_components`(I2 成立,grep 审计无旁路)。
- 4 入口事件都来自 `session.events()`(I3 成立);notifier/web-EventBus/MCP-_emit 三套收敛为一套(或后两者成为对单一总线的薄适配)。
- CLI/web 不再跳过 cpa m1–m6(bug 关闭,补一条「CLI 启用 M2/M3/M5」的回归断言)。

---

## 4. 关节 B 契约:`ChainContext` + registry 分发(编排 ↔ 策略)

### 4.1 现状落点(真实代码)
- 已有 `ChainContext`/兼容别名 `StrategyContext`(`strategy_registry.py:14`):`{dispatcher:Any, target, page_features, hint, extras, state, runtime, capability_registry, strategy_memory, exploitation_mode}` —— **显式字段已叠加,但 `dispatcher:Any` 仍作为过渡期兼容能力透传**。
- 已有 `StrategyDefinition`(`strategy_registry.py:23`):`{kind, chain_name, precondition, execute, ...}` + `is_applicable(ctx)`。
- 分发现状:`_execute_chain`(`ctf_dispatcher.py:2291`)硬 if/elif on `chain_name`;约 60% chain 逻辑仍是 dispatcher 上的 `_execute_*/_attempt_*/_run_*_strategy` 方法(共 40 个),未进 registry。
- 结果类型:`_ChainOutcome{progress, flag, reason}`(`ctf_dispatcher.py:268`)。

### 4.2 目标契约(P0 仅定义,P3/P4 落地)
- **`ChainContext`**:由现有 `StrategyContext` 演进。把 chain 真正需要的状态**显式字段化**,替换 `dispatcher:Any` 透传:
  ```python
  @dataclass
  class ChainContext:
      target: str
      page_features: dict
      state: "CTFState"            # 显式,而非 dispatcher.state
      strategy_memory: "StrategyMemory"
      runtime: "Runtime"
      capability_registry: "CapabilityRegistry"
      exploitation_mode: str       # aggressive / conservative
      hint: str = ""
      extras: dict = field(default_factory=dict)
  ```
  过渡期允许保留 `dispatcher` 引用做兼容,但新代码只准用显式字段(I4);存量逐 chain 收敛。
- **registry 驱动分发**:`_execute_chain` 改为按 `chain_name` 从 `StrategyRegistry` 取该链有序策略并跑 `_run_strategy_sequence`,取代 if/elif。保留现有语义:**仅 `outcome.flag` 短路,progress 不短路**(这是已验证 4 次的「末尾追加桥接策略零回归」前提,不能改)。
- **chains/ 子包**:dispatcher 上的 40 个 chain 方法物理迁出到 `flaghunter/agents/pa_agent/chains/{web,sqli,xss,ssti,upload,jwt,misc,cmdi,ssrf,lfi}.py`,每个注册为 `StrategyDefinition`。`ctf_dispatcher.py` 收缩为「装配 + 主循环 + 路由」。

### 4.3 关节 B「完成定义(DoD)」
- `_execute_chain` 无 chain 专属 if/elif(除最外层 registry 查询)。
- registry 覆盖率从 ~40% 提升到目标 100%(所有 chain 方法皆 `StrategyDefinition`)。
- 新增 chain 不再访问 `dispatcher.*` 隐式状态(`ChainContext` 显式传)。
- `ctf_dispatcher.py` 行数显著下降(目标主文件 < 2k 行,其余进 chains/)。

---

## 5. 自顶向下落地顺序

| 阶段 | 范围 | 关节 | 风险 | 主要 DoD |
|---|---|---|---|---|
| **P0** | 本 ADR:定骨架 + 两契约(纸面) | — | 无 | 经确认 |
| **P1** | `AgentSession` 落地;4 入口改走门面;修 CLI/web cpa-skip bug | A | 中(触 TUI/web 大文件) | §3.3 |
| **P2** | pa_agent & crew 校验同一事件/结果契约 | A→编排 | 低 | 两 agent 输出经同一总线 |
| **P3** | P3a 已完成:`_execute_chain` if/elif → handler map;P3b 渐进引入 `ChainContext` 破上帝对象 | B | 中高 | §4.3 前两条 |
| **P4** | 拆 `chains/` 子包,低耦合先行(misc/cmdi/ssrf/lfi → sqli/jwt/upload → web/xss/ssti) | B | 中高 | §4.3 后两条 |
| **P5** | cpa_modules m1..m6 命名/文档、capability registry 收尾 ✅(2026-06-20,卡 B,见 §5.2「P5(命名/文档线)收尾」) | 能力层 | 低 | 模块职责对照表(`docs/dev/cpa_modules_m1-m6_职责对照表_2026-06-20_V1.md`) |

每阶段硬性 gate(沿用现有纪律):
- full unit suite 零新增失败(deselect 2 个 `*_on_kali` 测试);必要时跑 integration。
- 逻辑拆 commit;conventional 前缀 + `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`;直推 main。
- 每个对外行为变更配 live 或单测回归证据。
- **不提交 `challenges/`。**

---

## 5.1 已清理:组合根错位(P1-b/P2)

`build_agent_components`(及 `build_runtime`)本质是**组合根**(composition root):向下 import agents/llm/tools/runtime 装配一切,理应被所有入口依赖、位置在 entry 层之下。当前代码已将组合根迁入 `session/initializer.py`,避免 `AgentSession` 反向 import `interface` 层(满足 I1)。

- **P1-a 临时处置(已落地)**:`AgentSession.create` 通过**延迟(函数内)import** 或注入式 `builder` 参数引用组合根,**无模块级环**;单测用 fake builder 注入。
- **P1-b/P2 正解(已落地)**:`build_agent_components/build_runtime/activate_workspace_for_target/has_ssh_runtime_config` 已迁到 `session/initializer.py`;`interface/initializer.py` 退为 re-export 保后向兼容;`AgentSession.create` 默认从 session-owned composition root 延迟 import。

## 5.2 进展日志

- **P0 完成**(commit `65de99a`):本 ADR + 契约选型。
- **P1-a 完成**(commit `d6c1712`):`session/event_bus.py`(中立 EventBus,I3)+ `session/agent_session.py`(`AgentSession` 门面 + `RunResult`,I2),10 单测,零回归(1521 passed)。独立新代码,未触入口。
- **P1-b 进行中(CLI 已迁)**:`interface/cli.py` 三模式(ctf/crew/default)全部经 `AgentSession.create` 装配,删除手工 `build_runtime/LLM/get_all_tools/PentestAgentAgent` 构造 → **CLI 的 CPA M1–M6 不再被跳过**(bug 关闭)。新增结构守卫单测 `test_cli_uses_agent_session.py` 锁定 I2;原 `test_cli_local_asset_contract.py` 的 4 个 run_cli 测试改打桩到门面 seam(`AgentSession.create`)。CTF 模式 LLM 仍是 `temperature=0.7`(与门面一致)→ **行为零扰动**。
- **P1-web 完成**:装配 `77a2618`(修 CPA-skip)+ EventBus 统一 `c25d967`(web 私有 EventBus → 中立适配器)。
- **P1-mcp 完成** `6a0ca41`:MCP 本就经 `build_agent_components`(无 CPA bug),纯 I3——`_emit`/`_ui_hook` → 中立 EventBus。
- **P1-tui 完成** `c25f747`:**关键发现**——TUI 本就经 `build_agent_components`(无 CPA bug);I3 只需把 `notifier.notify` 通道 bus 化(TUI 一行未动,`register_callback` 自动订阅);3 个控制通道(spawn/despawn/wake-up,带返回值的定向 hook)保持原样。
- **关节 A 全闭合**:I2 全线达成(CLI/web 修了 bug,MCP/TUI 本就合规);I3 全线达成(web/MCP/notifier 均收敛到中立 EventBus)。
- **P2 顺带达成**:crew(orchestrator/worker_pool)与单 agent 都走 `notify()` → 随 notifier bus 化即同源;`RunResult` 结果契约在 `AgentSession`。
- **P1-b/P2 组合根下沉完成**:`session/initializer.py` 成为中立组合根,`interface/initializer.py` 仅作兼容 re-export;新增 `test_default_builder_lives_below_interface_layer` 与 `test_interface_initializer_is_compatibility_reexport` 锁定该契约。
- **P3a 完成**:`_execute_chain` 收缩为 handler map 路由 + 统一 LLM fallback;XSS/Web 共享预跑语义下沉到 `_execute_xss_route/_execute_web_route`;新增 `test_execute_chain_routes_through_handler_map_without_chain_specific_branches` 防止 chain 专属分支回流。
- **P3b 第一刀完成**:`StrategyContext` 演进为 `ChainContext`(保留 `StrategyContext` 兼容别名),显式携带 `state/runtime/capability_registry/strategy_memory/exploitation_mode`;`CTFTaskDispatcher._strategy_context()` 统一填充这些字段;SSTI/hash/render 相关 precondition 已优先读 `context.state`,避免新增 `dispatcher.state` 直读。
- **P4 低耦合链拆分完成(8 chain mixin 全部迁出)**:`cmdi/ssrf` 已在 `chains/injection.py`;LFI 固定文件读取探针已迁入 `chains/file_read.py::LFIChainMixin`;misc registry 顺序执行 + LLM fallback 已迁入 `chains/misc.py::MiscChainMixin`;JWT 薄路由已迁入 `chains/jwt.py::JWTChainMixin`;upload 通用上传编排入口已迁入 `chains/upload.py::UploadChainMixin`;SQLi registry/sqlmap 编排入口已迁入 `chains/sqli.py::SQLIChainMixin`;XSS 路由 + 链入口已迁入 `chains/xss.py::XSSChainMixin`;Web 路由 + 固定策略序列入口已迁入 `chains/web.py::WebChainMixin`,SSTI 三段管线仍作为 Web 策略序列保留。dispatcher 对这些链只保留 mixin 组合和 handler 委托。新增 `test_lfi_chain_handler_is_delegated_to_file_read_chain_mixin` / `test_lfi_chain_mixin_builds_existing_probe_commands` / `test_dispatcher_misc_chain_method_comes_from_misc_mixin` / `test_dispatcher_jwt_chain_handler_delegates_to_jwt_mixin` / `test_upload_chain_entrypoint_lives_in_upload_chain_mixin` / `test_sqli_chain_entrypoint_lives_in_sqli_chain_mixin` / `test_xss_chain_entrypoints_live_in_xss_chain_mixin` / `test_web_chain_entrypoints_live_in_web_chain_mixin` 锁定物理迁出和等价入口。
- **P5 第一刀(执行体迁出)完成**:把侦察执行体 `_phase_recon`(约 327 行)从 `ctf_dispatcher.py` 物理迁入新文件 `pa_agent/recon_executor.py::ReconExecutorMixin`,沿用 P4 的混入类套路(方法体逐字搬运、`self.*` 运行时解析、调用点 `coordinator._apply_recon_contract -> dispatcher._phase_recon` 不变)=纯代码搬家近零风险。配套把 recon 专用正则常量 `_SCRIPT_SRC_RE`/`_BACKUP_CLUE_RE` 下沉到无环的 `dispatcher_helpers.py`(经 `import *` re-export,避免 mixin↔dispatcher 循环 import)。`ctf_dispatcher.py` 行数 9733→9405(−328)。新增 `test_recon_phase_lives_in_recon_executor_mixin` 锁定模块归属。**注意**:这是 Workstream A 的安全增量(物理迁出),尚未做激进版——coordinator 仍通过透明 `RunContext` 调 `dispatcher._phase_recon`,真正"coordinator 持有 executor 实例、断开 `self.*` 透传"需重写 contract 依赖注入,高风险,留待后续。
- **P5 第二刀(post-auth recon/auth 表单簇)完成**:`_candidate_auth_page_urls`/`_harvest_auth_forms_from_routes`/`_attempt_post_auth_recon`/`_find_registration_form`/`_build_account_form_submission`(333 行)迁入 recon_executor。新依赖 `find_auth_form` 从无环的 `.ctf_planner` import,表单 helper 从 `dispatcher_helpers` import;`_form_action_url`/`_is_legacy` 留 dispatcher。9405→9074(−333)。守卫扩展 `test_post_auth_recon_methods_live_in_recon_executor_mixin`。
- **P5 第三刀(exploration agenda 簇)完成**:`_populate_exploration_agenda_from_recon`/`_seed_framework_conventional_routes`/`_explore_agenda_items`(120 行)迁入 recon_executor,零新 import(符号已齐),self 依赖(`_should_ignore_exploration_candidate`/`_classify_exploration_hint_strength`/`_scan_and_store`)与类属性 `_FRAMEWORK_CONVENTIONAL_ROUTES` 留 dispatcher 经 MRO 解析。9074→8954(−120)。守卫扩展 `test_exploration_agenda_methods_live_in_recon_executor_mixin`。recon 簇主体迁移完成,`recon_executor.py` 现约 817 行;`ctf_dispatcher.py` 自 P5 起 9733→8954(−779)。
- **P5 第四刀(LLM 执行体簇)完成**:LLM 驱动探索/动作执行的 15 方法连续簇(`_run_llm_driven_exploration`→`_expected_signal_met`,734 行)迁入新文件 `pa_agent/llm_executor.py::LLMExecutorMixin`。调研确认**无障碍符号**:依赖全部已 import 或在 helpers(`parse_llm_json`/`_ChainOutcome`/`LLMStepLog`/`PreActionReasoning`/`StrategyContext`/`_base_target` + stdlib),`project_blackboard` 保持簇内动态导入避免循环。14 方法仅簇内调用,`_run_llm_driven_exploration` 被簇外 2 处(`_execute_chain`、SSTI)调用,mixin self 解析无影响。两个 @staticmethod(`_extract_llm_action_text`/`_looks_like_loopback_or_file_target`)装饰器保留。8954→8220(−734)。守卫 `test_ctf_llm_executor.py`。自 P5 起累计 9733→8220(−1513)。
- **P5 第五刀(flag 文本/拒绝/候选簇)完成**:`_extract_flag`/`_extract_php_var_dump_strings`/`_extract_runtime_flag`/`_is_rejected_flag`/`_looks_like_css_false_flag`/`_load_rejected_flags`/`_store_flag_candidate`(7 方法连续块,152 行)迁入新文件 `pa_agent/flag_parser.py::FlagParserMixin`。前置下沉障碍符号 `_FLAG_RE`/`_STRICT_FLAG_RE` 到 `dispatcher_helpers.py`(+`__all__`,mixin 显式 import);`get_all_notes_sync` 已在顶部 import,`self.state`/`_store_note` 留 dispatcher。8220→8063(−157,含删常量)。守卫 `test_ctf_flag_parser.py`。**注意**:flag 簇里 `_observe_flag`/`_record_wrong_flag_feedback`/`_hydrate_flag_proof`(+辅助 `_recent_relevant_observations`/`_observation_reference`/`_format_reproduction_steps`)物理分散且 hydrate 带辅助簇,留后续刀(它们同样可 mixin 迁出——调研 agent "依赖重不可迁"的判断不成立,mixin 不定义 `__init__`、`self.verifier` 等运行时解析,与已迁 LLM 簇调 runtime 同理)。自 P5 起累计 9733→8063(−1670)。
- **P5 第六刀(JWT helper 簇)完成**:连续 6 方法块(`_collect_candidate_jwts`/`_jwt_mutation_candidates`/`_jwt_algorithm_candidates`/`_jwt_secret_candidates`/`_encode_none_jwt`/`_jwt_request_headers`,约 158 行)迁入新文件 `pa_agent/jwt_executor.py::JWTExecutorMixin`。`_jwt_encode` 已在 `dispatcher_helpers.__all__` 经 re-export → **无障碍符号需下沉**,mixin 仅 `import re`/`Any`/`_jwt_encode`;`self.state`/`_recent_local_source_hint_secret_candidates` 留 dispatcher 经 MRO 解析。注意离群的 `_jwt_target_candidates`(line ~1125)非本簇,未动。8061→7903(−158)。守卫 `test_ctf_jwt_executor.py`。自 P5 起累计 9733→7903(−1830)。
- **P5 第七刀(platform 簇)完成**:连续 5 方法块(`_snapshot_platform_context`/`_infer_platform_profile`/`_infer_challenge_id`/`_align_platform_challenge`/`_build_already_solved_reason`,约 262 行)迁入新文件 `pa_agent/platform_executor.py::PlatformExecutorMixin`。`flag_submitter` 系列 import 保持簇内动态导入(原样)→ **无障碍符号需下沉**,mixin 仅 `import os`/`re`/`Any`/`urlparse`/`parse_qs`;`self.state`/`platform_orchestrator`/`_infer_*`(簇内)经 MRO 解析。注意紧随其后的 `_record_wrong_flag_feedback` 属 flag 簇(下一刀候选),未动。7903→7641(−262)。守卫 `test_ctf_platform_executor.py`。自 P5 起累计 9733→7641(−2092)。
- **P5 第八刀(flag-proof 簇)完成**:连续 5 方法块(`_record_wrong_flag_feedback`/`_hydrate_flag_proof`/`_recent_relevant_observations`/`_observation_reference`/`_format_reproduction_steps`,约 205 行)迁入新文件 `pa_agent/flag_proof.py::FlagProofMixin`。先派 Explore agent 测绘:确认**零障碍符号**(只用 `typing.Any` + 已 import 的 `FlagProof`),物理连续(6727–6932);依赖 `self.state`/`hypothesis_engine`/`_store_note`/`_pending_wrong_flag_feedback`/`_current_fingerprint` 全经 MRO 解析。入口枢纽 `_observe_flag`(离群 174 行,~6448)留 dispatcher,经 MRO 调到这些方法。7641→7436(−205)。守卫 `test_ctf_flag_proof.py`。自 P5 起累计 9733→7436(−2297)。
- **P5 第九刀(note/artifact 存储簇)完成**:连续 6 方法块(`_store_secret_note`/`_store_missing_tools`/`_store_retrospective`/`_store_note`/`_derive_artifact_producer`/`_derive_artifact_category`,约 141 行)迁入新文件 `pa_agent/note_store.py::NoteStoreMixin`。**零障碍符号**:依赖全部已 import(`typing.Any`/`urlparse`/`build_missing_tools_recorded_event`/`notes_tool`),无需下沉常量;`self.*`(`state`/`runtime`/`reasoning_layer`/`_record_session_event`/`_select_hypothesis_for_chain`/`_register_artifact_record`/`_notes_log`/`_emit`)全经 MRO 解析。簇内 `_store_note` 被簇外多处调用,迁出后经 MRO 等价解析无影响。7438→7297(−141)。守卫 `test_ctf_note_store.py`。自 P5 起累计 9733→7297(−2436)。**全套件 469 passed 零回归。**
- **P5 第十刀(flag 验证入口 `_observe_flag`)完成**:单方法(105 行)迁入新文件 `pa_agent/flag_observer.py::FlagObserverMixin`。先派 Explore agent 测绘:确认它是全仓 30+ 处调用的**唯一 flag 验证入口**,与紧邻的 5 个物理邻居(`_apply_submit_profile`/`_record_uniform_failure_surface`/`_restore_context`/`_select_hypothesis_for_chain`/`_snapshot_flag_counts`)语义无关(分属 submit-profile/strategy-memory/checkpoint/hypothesis-routing/progress-delta 子系统的"杂物抽屉"),逐一排除——其中 `_select_hypothesis_for_chain` 还会引入 `_CHAIN_NAME_FOR_HYPOTHESIS` 下沉成本,更不该捆迁。**零障碍符号**:`build_verification_decision_event` 直接从无环叶子 `harness.audit_events` import,`urlparse` 用 stdlib,无需下沉。`self.*` 全经 MRO 解析:`_hydrate_flag_proof`/`_record_wrong_flag_feedback` 在 FlagProofMixin、`_store_note` 在 NoteStoreMixin、`_record_session_event`/`state`/`verifier`/`_active_*_context` 留 dispatcher;`flag_proof.py` docstring 早已声明"`_observe_flag` 留 dispatcher 经 MRO 调到这些方法",本刀把"留 dispatcher"改为"留另一 mixin",MRO 行为完全等价。30+ 调用点(dispatcher/coordinator/chains/llm_executor)零改动。7297→7193(−104)。守卫 `test_ctf_flag_observer.py`。自 P5 起累计 9733→7193(−2540)。**全套件 470 passed 零回归。**
- **P5 第十一刀(progress-delta 簇)完成**:紧耦合连续对 `_snapshot_flag_counts`(16 行)+ `_derive_progress_delta`(34 行)迁入新文件 `pa_agent/progress_tracker.py::ProgressTrackerMixin`。这是第十刀测绘时点名排除的"杂物抽屉"里**唯一语义自洽的一对**(`_derive_progress_delta` 调 `_snapshot_flag_counts` 算假设引擎的进度信号)。**零障碍符号**:两方法只读 `self.state`(`candidate_flags`/`runtime_flags`/`verified_flags`/`rejected_flags`/`observations`),不引用任何模块级 import/常量,新文件仅需 `from __future__ import annotations`;`chain_outcome` 是 duck-typed 参数(`_ChainOutcome`)无需 import。调用点全经 MRO 不变:`_snapshot_flag_counts`(dispatcher ~462 + 簇内)、`_derive_progress_delta`(coordinator ~1300 `dispatcher._derive_progress_delta(...)`)。7193→7143(−50)。守卫 `test_ctf_progress_tracker.py`。自 P5 起累计 9733→7143(−2590)。**全套件 471 passed 零回归。**
- **P5 第十二刀(审计/持久化基础设施簇)完成**:文件末尾连续 13 方法块(`_setup_session_ledger`/`_setup_artifact_registry`/`_setup_checkpoint_store`/`_record_session_event`/`_write_checkpoint`/`_register_artifact_record`/`_record_recovery_decision`/`_resolve_registered_local_challenge_paths`/`_resolve_registered_local_key_files`/`_ingest_registered_local_source_hints`/`_runtime_browser_action`/`_runtime_proxy_action`/`_runtime_execute_command`,约 297 行)迁入新文件 `pa_agent/audit_infra.py::AuditInfraMixin`。先派 Explore agent 对 7143 行做全局结构测绘,在三个候选(审计基础设施 / render-surface+strategy-context / upload 表单助手)中选定本簇:**职责最单一**(整块即"审计/账本/检查点/注册表底座",全部经唯一汇聚点 `_record_session_event` 出口),**零障碍符号、零前置下沉**——所有模块级依赖(`SessionLedger`/`ArtifactRegistry`/`CheckpointStore` + 五个 `build_*_event`)均从无环 `harness.*` 直接 import(与 `note_store.py` 走的 `harness.audit_events` 同路径,已证无循环)。后备属性(`_session_ledger`/`_artifact_registry`/`_checkpoint_store`/`_ledger_run_id`/`_artifact_run_id`/`_checkpoint_run_id`/`_registered_local_source_hints_loaded`)留 `__init__` 初始化,经 `self` 由 MRO 解析;~105 处调用点全是 `self.*` 零改动。紧邻的 UI 透传 `_emit` 非审计职责,留 dispatcher 保持 mixin 纯净。`AuditInfraMixin` 置于继承列表首位。7143→6846(−297)。守卫 `test_ctf_audit_infra.py`。自 P5 起累计 9733→6846(−2887)。**全套件 472 passed 零回归。**
- **P5 第十三刀(render-surface 启发式簇)完成**:类尾部连续 9 方法块(`_collect_render_surface_urls`/`_normalize_render_surface_url`/`_strategy_surface_signature`/`_was_strategy_surface_exhausted`/`_mark_strategy_surface_exhausted`/`_response_fingerprint`/`_inject_render_payload`/`_extract_cookie_secret_candidate`/`_collect_candidate_filenames`,约 190 行)迁入新文件 `pa_agent/render_surface.py::RenderSurfaceMixin`。先派 Explore agent 对 cut-12 后的 6846 行复测,在 Candidate B(render-surface+strategy-context)与 Candidate C(upload 表单助手)间选定本簇,并**主动拆分** Candidate B:render-surface 9 方法只对 `self.state.observations` 做 URL 规范化/穷尽追踪/响应指纹/cookie-secret 与文件名提取,**与 strategy_registry 零耦合、两半之间无内部调用边**;紧邻的 strategy-context 对(`_strategy_context`/`_strategies_for_chain`)耦合 `StrategyContext`/`self.strategy_registry`、仅 2 方法不值单开文件,留作未来 strategy-registry 相邻抽取的伴侣,本刀只取 render-surface 半区。**零障碍符号、零下沉**:全部依赖仅 `re` + `urllib.parse`(`parse_qs`/`urlencode`/`urljoin`/`urlparse`)+ `typing.Any`,无模块级常量(`common_skip`/`sentence_patterns` 等均为方法内字面量);`self.state` 与簇内 `_normalize_render_surface_url`/`_was_strategy_surface_exhausted` 互调经 MRO 解析,调用点零改动。`RenderSurfaceMixin` 置于 `ProgressTrackerMixin` 与链 mixin 之间。6846→6656(−190)。守卫 `test_ctf_render_surface.py`。自 P5 起累计 9733→6656(−3077)。**全套件 473 passed 零回归。**
- **P5 第十四刀(upload 专属助手回折,非新文件)完成**:与前 13 刀"迁出到新 mixin 文件"不同,本刀是**回折**——把 5 个 upload-专属方法(`_form_action_url`/`_default_upload_form_fields`/`_generic_upload_payloads`/`_upload_followup_urls`/`_follow_uploaded_payloads`,约 127 行)从 `ctf_dispatcher.py` 迁入**既有** `chains/upload.py::UploadChainMixin`(`_execute_upload_chain` 本就调用这 5 个,且 upload.py 已经 `self` 用到 `_runtime_proxy_action`/`_scan_and_store`/`_extract_flag`/`_observe_flag`,回折不引入任何新跨 mixin 耦合)。**关键边界判断**:紧邻的 `_submit_form_request` 是 11 处共享(contact-form/SQLi/upload)的通用表单提交助手,**不属 upload 语义**,若折进 upload 命名 mixin 会让通用助手错置并使 SQLi/contact 链语义依赖 `UploadChainMixin`——故**显式留在 dispatcher**。upload.py 补 import `re`/`urljoin`/`_normalize_exploration_url`(后者已从 `dispatcher_helpers` 导出,无下沉);`_extract_embedded_links` 仍留 dispatcher 经 MRO 解析。6656→6529(−127)。守卫 `test_ctf_upload_fold.py`(双断言:5 方法归属 upload mixin + `_submit_form_request` 仍归属 dispatcher)。自 P5 起累计 9733→6529(−3204)。**全套件 475 passed 零回归。**
- **P5 第十五刀(SSTI 策略执行体簇)完成**:进入中部策略执行体后的第一刀,连续 6 方法块(`_ssti_exploitation_gated_by_mode` + `_run_render_parameter_ssti_strategy`/`_run_tornado_ssti_strategy` + Phase 7 三段流水线 `_run_ssti_probe_strategy`/`_run_ssti_identify_strategy`/`_run_ssti_exploit_strategy`,约 585 行)迁入新文件 `pa_agent/ssti_executor.py::SSTIExecutorMixin`。**这是迄今单刀载荷最大的一次(−585)**。先派 Explore agent 对中部 `_run_*`/`_attempt_*`(~1596–5935)做专门的"常量共享测绘":逐一 grep 顶部 11 个模块级常量的全仓引用,确认它们各自**只被单一簇引用、且全部留在 ctf_dispatcher.py**——决定性结论是 SSTI 簇**零常量引用**,是中部成本最低的刀(SQLi 簇次之但需随带 only-this-cluster 的 `_SQLI_AUTH_BYPASS_PAYLOADS`)。**边界判断**:`_ssti_exploitation_gated_by_mode` 虽也被紧随其后的 `_run_hash_guarded_file_read_strategy`(hash 簇,留 dispatcher)调用,但它是 SSTI 专用门控、物理紧贴 SSTI 块,纳入 SSTI mixin 后 hash 策略经 MRO 仍正确解析,故随簇迁出更内聚。**零障碍符号、零下沉**:仅 `_ChainOutcome`(`.chains.base`)+ `_base_target`(`dispatcher_helpers`)+ stdlib `re`/`urljoin`/`urlparse`/`Any`;render-surface 七助手(已在 RenderSurfaceMixin)、`_extract_flag`/`_observe_flag`/`_run_llm_driven_exploration` 及留 dispatcher 的 `_run_hash_reconstruction_attack_strategy`/`_scan_and_store`/`_record_uniform_failure_surface`/`_strategy_context` 全经 MRO 解析。入口经 strategy_registry/chains 调用零改动。`SSTIExecutorMixin` 置于 `RenderSurfaceMixin` 与链 mixin 之间。6529→5944(−585)。守卫 `test_ctf_ssti_executor.py`。自 P5 起累计 9733→5944(−3789)。**全套件 476 passed 零回归。**
- **P5 第十六刀(SQLi 策略执行体簇)完成**:连续 5 方法块(`_run_unicode_numeric_form_bypass_strategy`/`_attempt_auth_form_sqli`/`_attempt_sqlmap_sqli`/`_attempt_generic_param_sqli`/`_attempt_local_challenge_log_pivot`,约 495 行)迁入新文件 `pa_agent/sqli_executor.py::SQLiExecutorMixin`。**首次随簇搬运一个模块级常量**:`_SQLI_AUTH_BYPASS_PAYLOADS`(顶部 line 126)仅被本簇 `_attempt_auth_form_sqli` 引用(only-this-cluster,中部测绘已证),故直接把字面量移入新文件、从 ctf_dispatcher.py 顶部删除(留一行迁移注释),**无需下沉到 dispatcher_helpers**。其余模块级符号全部已在无环模块:11 个表单/sqlmap/本地题助手(`_pick_form_field`/`_looks_like_successful_auth_change`/`_build_sqlmap_target_from_form`/`_quote_sql_identifier`/`_extract_local_challenge_root`/`_resolve_compose_file`/`_docker_compose_logs_command`/`_extract_admin_password_from_logs`/`_resolve_auth_login_url`/`_extract_sid_from_login_response`/`_base_target`)从 `dispatcher_helpers` 显式 import、`find_auth_form` 从 `ctf_planner`、`_ChainOutcome` 从 `chains.base`;`run_sqlmap` 保持方法内动态 import。**关键 self 解析**:共享的 `_submit_form_request`(第十四刀刻意留 dispatcher)+ `_scan_and_store`/`_emit`/`_recent_local_source_hint_routes`/`_fetch_admin_with_sid`(留 dispatcher)、`_extract_flag`/`_extract_php_var_dump_strings`(FlagParser)、`_observe_flag`(FlagObserver)、`_store_note`/`_store_secret_note`(NoteStore)、`_resolve_registered_local_challenge_paths`(AuditInfra)全经 MRO 解析。入口经 strategy_registry/chains 调用(`_attempt_sqlmap_sqli`←chains/sqli、`_attempt_local_challenge_log_pivot`←chains/xss 等 5 处)零改动。`SQLiExecutorMixin` 置于 `SSTIExecutorMixin` 后。5944→5447(−497,含删常量)。守卫 `test_ctf_sqli_executor.py`(双断言:5 方法归属 + 常量随簇迁移)。自 P5 起累计 9733→5447(−4286)。**全套件 478 passed 零回归。**
- **P5 第十七刀(hash-guarded / backup-leak 簇)完成**:连续 6 方法块(`_run_hash_guarded_file_read_strategy`/`_run_hash_reconstruction_attack_strategy`/`_run_backup_source_leak_strategy`/`_looks_like_warmup_include_source`/`_attempt_warmup_include_bypass`/`_extract_warmup_flag_filenames`,约 555 行)迁入新文件 `pa_agent/hash_backup_executor.py::HashBackupExecutorMixin`。**随簇搬运两个 only-this-cluster 常量**:`_COMMON_BACKUP_PATHS` + `_DJANGO_STATIC_SOURCE_PROBES`(仅用于 `_run_backup_source_leak_strategy` 内 2 处)移入新文件、从 dispatcher 顶部删除,无需下沉。其余模块级符号全部已在无环 `dispatcher_helpers`(`_BACKUP_CLUE_RE`/`_infer_hash_format`/`_discover_hash_params`/`_compute_signed_hash`/`_looks_like_inline_source_leak`/`_looks_like_python_source_leak`/`_looks_like_backup_source_candidate`/`_looks_like_archive_path`/`_strip_html_text`/`_normalize_exploration_url`/`_base_target`)+ `_ChainOutcome`。**与已抽 SSTI mixin 的双向跨 mixin 调用**:本簇 `_run_hash_guarded_file_read_strategy` 调 SSTI mixin 的 `_ssti_exploitation_gated_by_mode`/`_run_ssti_probe_strategy`,而 SSTI mixin 又调本簇的 `_run_hash_reconstruction_attack_strategy`——两 mixin 互调全经 dispatcher 的 MRO 解析,行为等价(印证 mixin 拆分不破坏跨簇协作)。render-surface 助手、`_extract_flag`/`_observe_flag`/`_store_note` 及留 dispatcher 的 `_scan_and_store`/`_emit`/`_recent_source_hint_backup_probe_paths`/`_download_and_analyze_backup_artifact` 全经 MRO 解析。`HashBackupExecutorMixin` 置于 `SQLiExecutorMixin` 后。5447→4870(−577,含删两常量)。守卫 `test_ctf_hash_backup_executor.py`(双断言:6 方法归属 + 两常量随簇迁移)。自 P5 起累计 9733→4870(−4863,**已破 5000 行、累计砍掉一半**)。**全套件 480 passed 零回归。**
- **P5 第十八刀(PHP/SSRF/photo 链簇)完成**:连续 6 方法块(`_download_and_analyze_backup_artifact`/`_attempt_php_unserialize_chain`/`_attempt_php_upload_cookie_pop_chain`/`_attempt_source_fetch_write_ssrf_chain`/`_attempt_profile_photo_poisoning_chain`/`_extract_profile_base64_blob`,约 920 行)迁入新文件 `pa_agent/php_exploit_chain.py::PHPExploitChainMixin`。**这是迄今单刀最大的一次(−919),dispatcher 首次跌破 4000 行**。**零模块级常量**;模块级符号仅 `_ChainOutcome` + 三个无环 dispatcher_helpers(`_base_target`/`_pick_python_command`/`_with_query`)+ stdlib。**嵌套 def 核查**:`_download_and_analyze_backup_artifact` 内的 `scan_text` 实为 `script = r\"\"\"...\"\"\"` 字符串字面量里的远程分析脚本(非真 Python 嵌套),`_attempt_php_upload_cookie_pop_chain` 内的 `php_s`/`php_prop` 是真嵌套但仅用局部变量+字符串格式化、不引用外层模块符号——均随方法逐字搬运无碍。为避免 ~920 行转写错误,**用脚本从源文件精确抽取该 6 方法行拼成新文件**,先 `py_compile` 校验再从 dispatcher 删除同一区间。**跨 mixin 调用**:刚抽出的 hash-backup mixin 的 `_run_backup_source_leak_strategy` 经 MRO 调本簇 `_download_and_analyze_backup_artifact`;`_runtime_*`(AuditInfra)、`_extract_flag`/`_extract_runtime_flag`/`_store_flag_candidate`(FlagParser)、`_observe_flag`(FlagObserver)、`_store_note`(NoteStore)、共享 `_submit_form_request`(留 dispatcher)及 `_scan_and_store`/`_emit`/`_register_runtime_source_hint`/`_strategy_context`/`_extract_followup_fetch_targets`/`_recent_local_source_hint_text` 全经 MRO 解析。`PHPExploitChainMixin` 置于 `HashBackupExecutorMixin` 后。4870→3951(−919)。守卫 `test_ctf_php_exploit_chain.py`。自 P5 起累计 9733→3951(−5782,约 −59%)。**全套件 481 passed 零回归。**
- **P5 第十九刀(artifact-forensics 簇)完成**:连续 6 方法块(`_run_artifact_forensics_strategy`/`_collect_artifact_candidates`/`_ingest_local_challenge_artifacts`/`_expand_artifact_candidates_from_directory_pages`/`_collect_attachment_research_context`/`_analyze_attachment_artifact`,约 810 行)迁入新文件 `pa_agent/artifact_forensics.py::ArtifactForensicsMixin`。随簇搬运 only-this-cluster 常量 `_ATTACHMENT_CLUE_RE`(仅用于 `_expand_...directory_pages`);其余模块级符号全部已在无环 `dispatcher_helpers`(`_base_target`/`_normalize_exploration_url`/`_resolve_compose_file`/`_iter_local_challenge_key_files`/`_build_local_challenge_root_summary_metadata`/`_extract_local_challenge_root_from_local_archive`/`_pick_python_command`)+ `_ChainOutcome`。**嵌套 def 核查(本刀关键)**:测绘点名的 `add_flag`/`scan_text`/`scan_bytes`/`analyze_sqlite_bytes` 经实读确认**全在 `_analyze_attachment_artifact` 的 `script = r\"\"\"...\"\"\"` 字符串字面量内**(out-of-process zip/sqlite/base64 取证脚本,非真 Python 嵌套);全簇唯一真嵌套是 `_collect_artifact_candidates` 内的 `_add`(仅用 `_normalize_exploration_url`+闭包局部)——均随方法逐字搬运无碍。脚本精确抽取 + `py_compile` 校验后再删。**踩坑修复**:首次用 bash heredoc 的非 raw `'''...'''` Python 字面量拼 `_ATTACHMENT_CLUE_RE` 时,`\\b` 被转义成退格符(0x08)、丢掉所有 `\b` 词边界——改为**从尚未删除的 dispatcher 原常量逐字节复制**,并加守卫断言锁词边界/中文语义(`.sqlite3` 命中、`附件压缩包` 命中、`plaindbword` 不命中),修复后与原值 byte-identical。`ArtifactForensicsMixin` 置于 `PHPExploitChainMixin` 后。3951→3140(−811,含删常量)。守卫 `test_ctf_artifact_forensics.py`(双断言:6 方法归属 + 正则语义完好)。自 P5 起累计 9733→3140(−6593,约 −68%)。**全套件 483 passed 零回归。**
- **P5 第二十刀(XSS / visit-url / docker-loopback collector 链簇)完成**:连续 6 方法块(`_attempt_stored_xss_chain`/`_attempt_visit_url_chain`/`_attempt_docker_loopback_visit_chain`/`_resolve_loopback_target_container`/`_handle_collector_hit`/`_fetch_admin_with_sid`,约 287 行)迁入新文件 `pa_agent/xss_collector_chain.py::XSSCollectorChainMixin`。**零模块级常量**(与 SSTI 簇并列为中部成本最低维度)。**唯一类依赖** `_CollectorServer`——并行 Explore 测绘已证 `collector_server.py` 仅依赖 `dispatcher_helpers`、无反向 import ctf_dispatcher,故新文件可直接 `from .collector_server import _CollectorServer`、**无循环依赖**;11 个 docker/sid helper(`_pick_form_field`/`_is_loopback_base`/`_target_port`/`_command_output_text`/`_extract_sid_from_collector_log`/`_extract_sid_from_text`/`_docker_loopback_probe_script`+copy/start/read/cleanup 四命令构造器)全在无环 `dispatcher_helpers`;`_ChainOutcome` + stdlib。**嵌套 def 核查**:无内层 `def`、无 `script = r"""..."""` 字面量;仅 `_attempt_stored_xss_chain` 内 2 个 payload **lambda**(真内层闭包,随方法搬)。**本刀关键修复(首跑 2 失败)**:符号真正归属新文件后,删去 dispatcher 顶部 `from .collector_server import _CollectorServer` 死 import;但 3 个 monkeypatch 点(`test_ctf_dispatcher.py` 两处 setattr-**rebind** + 集成测试 `test_easy_login_runtime_only_docker_fallback.py` 的 `.start`)仍指 `ctf_dispatcher._CollectorServer`——rebind 只改旧模块名、打不到搬走方法的查找点(`xss_collector_chain._CollectorServer`),致 `_attempt_visit_url_chain` 跑真实 collector 而非 fake、`instances` 断言失败。修复:把 3 个 patch 路径全部改指**使用点** `xss_collector_chain._CollectorServer`(改注入位置,不改被测行为;`.start` 因 pat 的是共享类对象上的方法,改模块路径语义等价)。**教训**:搬运被 monkeypatch 的模块级名字时,必须同步迁移所有 `setattr`-rebind 型 patch 的路径到新模块。**跨 mixin**:`_runtime_proxy_action`/`_runtime_execute_command`(AuditInfra)、`_observe_flag`(FlagObserver)、`_extract_flag`(FlagParser)、`_store_note`/`_store_secret_note`(NoteStore)、留 dispatcher 的 `_scan_and_store`/`_emit` 全经 MRO;**反向印证**:已抽的 `SQLiExecutorMixin._attempt_local_challenge_log_pivot` 经 MRO 调本簇 `_fetch_admin_with_sid`(两 mixin 共存同一 MRO,无碍)。实例状态 `_exhausted_visit_url_targets`/`collector_port` 留 `__init__`。`XSSCollectorChainMixin` 置于 `SSTIExecutorMixin` 与 `SQLiExecutorMixin` 之间。3140→2854(−286)。守卫 `test_ctf_xss_collector_chain.py`。自 P5 起累计 9733→2854(−6879,约 −71%)。**全套件 484 passed 零回归。**
- **P5 第二十一刀(JWT / hint / file-read / contact-report 策略簇)完成**:连续 7 方法块(`_run_jwt_manipulation_strategy`/`_run_hint_chain_followup_strategy`/`_run_file_read_endpoint_strategy`/`_run_contact_report_chain_strategy` + contact 提交三件套 `_has_prior_contact_report_submission`/`_is_contact_submission_success`/`_attempt_contact_captcha_bypass`,约 700 行)迁入新文件 `pa_agent/jwt_contact_chain.py::JWTContactChainMixin`。随簇搬运 only-this-cluster 常量 `_CONTACT_POW_CHALLENGE_RE`(仅 `_run_contact_report_chain_strategy` 一处引用);模块级符号仅 `_ChainOutcome` + 7 个无环 `dispatcher_helpers`(`_base_target`/`_jwt_get_unverified_header`/`_jwt_decode_payload`/`_jwt_encode`/`_parse_forms_from_html`/`_solve_contact_captcha_solution`/`_solve_contact_pow_solution`)+ stdlib。**离群方法不随迁**:并行测绘已证 `_jwt_target_candidates`、`_extract_followup_fetch_targets`(及其常量 `_HTML_TAG_PSEUDO_PATHS`、`_CHAIN_NAME_FOR_HYPOTHESIS`)在簇外、被簇外代码引用,留 dispatcher,经 MRO 解析。**两处工程化收获**:(1)**`\b`→退格符陷阱复发**——脚本抽取后用 heredoc 非 raw 字面量拼 `_CONTACT_POW_CHALLENGE_RE` 时 `\b` 再次被转成 0x08,沿用第十九刀对策**从 dispatcher 原行逐字节复制**修复(byte-correct `\b(\d+_[A-Za-z0-9]+)\b`),守卫加 `"\x08" not in pattern` 断言。(2)**新增 import 完整性闸**:本机 venv 无 ruff/pyflakes,改用标准库 `symtable` 做自由变量分析(逐方法收集引用模块全局的名字 − 本模块已定义名 − builtins = 漏导入,结果 NONE)+ body 正则扫描查 F401 多余导入——成为脚本抽取大块 mixin 后验证 import 闭合的可复用手段(py_compile 抓不到方法体里漏导的模块级符号)。**本刀关键修复(首跑 1 失败,同第二十刀 patch 路径模式)**:`test_ctf_dispatcher.py` 两个 contact-report 测试 setattr-rebind 的是 `ctf_dispatcher._solve_contact_captcha_solution`/`_solve_contact_pow_solution`,但搬走的方法查找 `jwt_contact_chain` 名,致 fake solver 未生效、断言失败;把 4 个 patch 路径改指使用点 `jwt_contact_chain._solve_*`(全仓确认两 solver 调用点仅本簇;测试顶部从 ctf_dispatcher re-export 消费 `_solve_contact_pow_solution` 及 5861 直调不受影响,不动)。**跨 mixin**:`JWTExecutorMixin` 六个 `_jwt_*`、`_observe_flag`/`_extract_flag`/`_store_note` 及留 dispatcher 的 `_scan_and_store`/`_submit_form_request`/`_record_uniform_failure_surface`/`_build_file_wrapper_urls`/`_decode_base64_source_blob`/`_jwt_target_candidates`/`_extract_followup_fetch_targets` 全经 MRO 解析。`JWTContactChainMixin` 置于 `XSSCollectorChainMixin` 后。2854→2154(−700,含删常量)。守卫 `test_ctf_jwt_contact_chain.py`(双断言:7 方法归属 + 正则语义完好且无 0x08)。自 P5 起累计 9733→2154(−7579,约 −78%)。**全套件 486 passed 零回归。**
- **P5 第二十二刀(source-hint 登记簇)完成 —— 收尾阶段第一刀**:连续 8 方法块(`_has_recent_local_source_hint`/`_recent_local_source_hint_count`/`_recent_source_hint_backup_probe_paths`/`_recent_local_source_hint_text`/`_recent_local_source_hint_routes`/`_recent_local_source_hint_secret_candidates`/`_looks_like_source_code_blob`/`_register_runtime_source_hint`,约 174 行)迁入新文件 `pa_agent/source_hint_registry.py::SourceHintRegistryMixin`——围绕 `local_challenge_source_hint` 状态观测的读写访问器,高内聚。随簇搬运 only-this-cluster 常量 `_SOURCE_HINT_BACKUP_PROBES`(仅 `_recent_source_hint_backup_probe_paths` 一处引用);模块级符号仅 `_looks_like_inline_source_leak`(无环 `dispatcher_helpers`)+ stdlib `re`/`Path`/`urlparse`,无 `_ChainOutcome`、无嵌套 def。**边界判断**:物理紧邻的 `_extract_followup_fetch_targets`(被 php_exploit_chain 引用)、`_jwt_target_candidates`(被 jwt_contact_chain 引用)是簇外 helper,留 dispatcher、经 MRO 解析。**流程改进生效(消除第二十/二十一刀的 patch-路径来回)**:抽取后、跑全套件前先做 monkeypatch 路径预扫——`grep "ctf_dispatcher\.<moved_symbol>"` 覆盖 8 方法名 + 常量 + helper,**零命中**,确认本刀无 setattr-rebind 型 patch 需迁移;遂一次跑通、无失败重跑。import 完整性仍用 `symtable` 自由变量分析(零漏导入)+ body 扫描(零多余),常量为纯 ASCII 字典经脚本 verbatim 搬运(无 `\b` 转义风险)。`SourceHintRegistryMixin` 置于 `JWTContactChainMixin` 后。2154→1976(−178,含删常量,**dispatcher 破 2000 行**)。守卫 `test_ctf_source_hint_registry.py`(双断言:8 方法归属 + 常量随簇迁移)。自 P5 起累计 9733→1976(−7757,约 −80%)。**全套件 488 passed 零回归。**
- **P5 第二十三刀(exploit-replay 记忆簇)完成 —— 收尾阶段第二刀**:连续 7 方法块(`_recent_observed_php_unserialize_source_exploit`/`_recent_observed_profile_photo_poisoning_exploit`/`_recent_observed_source_fetch_write_exploit`/`_recent_local_profile_photo_poisoning_source_exploit`/`_recent_local_php_unserialize_source_exploit`/`_recent_php_unserialize_source_exploit`/`_recent_profile_photo_poisoning_source_exploit`,约 220 行)迁入新文件 `pa_agent/exploit_replay_memory.py::ExploitReplayMemoryMixin`——从 observed/local 源码线索状态重建既有漏洞(php_unserialize / profile_photo_poisoning / source_fetch_write_ssrf)的只读访问器,纯内部互调(后两个聚合方法分别调前面的 observed+local 变体)。**零模块级常量、零 helper、零 `_ChainOutcome`、无嵌套 def**;障碍符号仅 stdlib `re`+`Any`。**`\x00` 保全验证**:`_recent_local_php_unserialize_source_exploit` 内 PHP 序列化 payload 的 4 处 `\x00`(非 raw 字符串里的 null 字节转义)经脚本 verbatim 切片原样保留(源文本仍是字面 `\x00` 四字符、非真 null 字节)——再次印证脚本切片优于字面量重打。monkeypatch 路径预扫(7 方法名)**零命中**,一次跑通。`ExploitReplayMemoryMixin` 置于 `SourceHintRegistryMixin` 后。1976→1758(−218)。守卫 `test_ctf_exploit_replay_memory.py`。自 P5 起累计 9733→1758(−7975,约 −82%)。**全套件 489 passed 零回归。**

### P5 收敛宣告(2026-06-20,第二十三刀后)
经第二十三刀后的**判断性收敛测绘**(Explore agent 全文通读 ctf_dispatcher.py 现 36 个方法,逐一归档),确认 **god object 已解体、P5 主线收敛收工**:

- **战绩**:`ctf_dispatcher.py` 9733 → 1758 行(**累计 −7975,约 −82%**),策略/执行体/攻击链/访问器载荷分 23 刀外迁到 ~17 个行为保持的 `XxxMixin`,全程 `self.*` 经 MRO 解析、调用点零改动,**每刀全 agents 套件零回归**(469 → 489 passed,增量均为新增守卫)。
- **收敛判据(为何停)**:剩余 1758 行已是**核心调度骨架(生命周期 `__init__`/`run`/`_run_solve_loop`/`_finalize_solve_result` + 链分发 `_execute_chain`/`_chain_handler_map` + strategy 装配 `_select_primary_strategy`/`_strategy_context`/`_restore_context`)+ 横切共享 helper(`_submit_form_request`/`_scan_and_store`/`_emit`/`_execute_terminal_commands`/`_proxy_get_with_retry` 等,经 MRO 被多个已抽 mixin 共用)+ 零散 state 写入器**。测绘里**唯一够"物理连续 + ≥3 方法"门槛的 link/exploration 簇(~186 行)经 grep 证实被 `recon_executor`/`artifact_forensics`/`chains.upload` 等 4 个已抽 mixin 反向调用**——抽出会形成"已抽 mixin → 依赖新 mixin"的耦合、破坏 P5 坚持的**单向 MRO + dispatcher 收口共享 helper**架构,故判 A(骨架)不判 B(可抽簇)。**B 档为空 = 再抽即"为拆而拆"**,触及第十二刀开场设定的边界。
- **合格线**:dispatcher 已只剩"一个 dispatcher 本该有的样子",无残留大型领域簇。**这就是合格的稳态停点。**
- **收尾卫生清理**:删除确认全仓零引用的死常量 `_WEBISH_TYPES`。保留的 only-this-file 常量(`_HTML_TAG_PSEUDO_PATHS`/`_STATIC_RESOURCE_SUFFIXES`/`_SERVER_SIDE_PATH_SUFFIXES`/`_CHAIN_NAME_FOR_HYPOTHESIS` 及类属性 `_FRAMEWORK_*`)均仅本文件/本类合理使用,不再迁移。
- **沉淀的可复用方法论**(供后续同类重构):① 大块用**脚本精确切片 + py_compile**,不手工转写;② 含正则/`\x00` 等转义的字符串**绝不经非 raw Python 字面量重构**(三次 `\b`→退格符踩坑),从原文件逐字节复制;③ 缺 ruff/pyflakes 时用标准库 **`symtable` 自由变量分析**做 import 完整性闸 + body 扫描查多余;④ 抽取后跑套件**前**先做 **monkeypatch 路径预扫**(`grep ctf_dispatcher.<moved_symbol>`),把 setattr-rebind 型 patch 一次性改到使用点,消除"失败→改→重跑"的 6 分钟来回。

**后续若仍要演进 dispatcher,应转入 P3b 路线(ChainContext 破透传),而非继续切 mixin。**

### P5(命名/文档线)收尾(2026-06-20,卡 B)
**消歧**:本条是 §5 路线图表 **P5 行**定义的「cpa_modules m1..m6 命名/文档、capability registry 收尾」(能力层,低风险,DoD=模块职责对照表),**与上面那条「P5 god-object 23 刀拆分 ctf_dispatcher」是两码事**——后者属 §5.2 进展日志的 dispatcher 重构线,已于第二十三刀后收敛宣告。

- **产出**:新增 `docs/dev/cpa_modules_m1-m6_职责对照表_2026-06-20_V1.md`——六模块逐行列职责 + 暴露能力(`init_mN`/`is_mN_enabled`/`register_*_commands`)+ 调用方,并把 `__all__`/导出 ↔ `session/initializer.py` 调用列表逐项对账成文。
- **capability registry 一致性结论**:六模块均定义 `init_mN`+`is_mN_enabled`(M1 在 initializer 真正调 `is_m1_enabled()` 门控,M2–M6 在 initializer 处按 env 门控、各自 `is_mN_enabled` 仍被运行时其它处消费)。**唯一悬空导出已修**:M5 的 `SharedBlackboard`/`PheromoneRouter`/`AgentMessenger`/`ConsensusMechanism` 原仅 `TYPE_CHECKING` 下导入、运行时 `import *` 直接 `AttributeError`,现加 PEP 562 `__getattr__` 懒加载使其真正可导入(保留"避免顶层循环依赖"原意)。M6 `register_turbo_commands` 确认全仓无调用方(`command_registry` 抽象未用,`/turbo` 由 TUI 硬编码分发),保留为预留接口并加「未接线」docstring,不改行为。**卡 J 复核确认(2026-06-21)**:register_turbo_commands 仍无调用方、command_registry 抽象仍无消费者;接线前置(统一 registry 体系)不存在,维持预留接口、未做接线(避免误入牵动全 M1–M6 elif 链的跨模块重构)。
- **命名/docstring 校准(均不改行为)**:补 `cpa_modules/__init__.py` 包级 docstring(原为空);改写 M6 模块 docstring 如实说明"init 走 session initializer 钩子、实际面是 `/turbo`、透明 wrapper 自动挂载属预留未接线"(原称"M0 侵入点自动挂载/透明加速"名实不符);校准 M3 顶部误导性注释。
- **待复核(仅记录,未改行为)**:M4 `init_m4` 的 `DataProtector(mask_ips=not mask_sensitive, ...)` 读起来与开关语义相悖(`CPA_M4_MASK_SENSITIVE=true` 默认下因 `DataProtector` 默认 `mask_ips=False` 无可见差异,异常仅在 `=false` 时显现)——疑似 bug 但语义有歧义,按卡纪律「仅明确 bug 才改+加测试」留作待复核,不在本低风险文档卡夹带行为变更。
- **守卫**:`tests/unit/config/test_cpa_modules_capability_registry.py`——锁定六模块 `init_mN`/`is_mN_enabled` 契约、非空 docstring、`__all__` 无悬空导出(防 M5 类回归)、M5 核心类运行时可导入。
- **零回归**:`tests/unit/agents/` 489 passed(与 P5 god-object 收敛同基线,新守卫在 `tests/unit/config/`)。

### P3 风险发现(深度测绘,2026-06-17)
`_execute_chain` 的 if/elif 已安全改为 handler map(**P3a 完成**)。但**关节 B 的「破上帝对象」(P3b)高风险**:所有注册策略通过 `context.dispatcher.*` 访问 dispatcher 全部能力(`_run_strategy_sequence` 每轮重建 `ctx=_strategy_context(dispatcher=self)`),strategy_registry 里 40+ lambda 全依赖 `ctx.dispatcher._run_*/_attempt_*/state/strategy_memory/...`。用 ChainContext 显式字段**替换** dispatcher 透传 = 重写这 40+ lambda + 所有 chain 方法,动 live 调过的 CTF 核心,**单测绿 ≠ 解题行为不变**。故 P3 拆分:
- **P3a**:`_execute_chain` if/elif → handler map 分发(已落地,行为等价、机械、安全)。
- **P3b**:ChainContext 破上帝对象——**已完成「叠加显式字段、保留 dispatcher 兼容」第一刀**;下一步逐 strategy/executor 把 `ctx.dispatcher._run_*` 下沉到 chains/ 或显式服务字段,不做一次性替换。

#### P3b 可行性复评(2026-06-20,P5 收尾后双 Explore 测绘)
P5 完工后对 P3b 透传面做了精确测绘,**当初「高风险一次性重写」的前提已被 P4 推翻**,P3b 实际可像 P5 一样逐刀渐进:
- **chains/ 子包已彻底脱离透传**:`chains/*.py` 对 `ctx.dispatcher.*` 的属性访问 **= 0 处**(P4 抽 mixin 时已全部改为 `self.`/MRO)。P3b **无需动 chains**。
- **透传面 100% 集中在 `strategy_registry.py` 一个文件**:27 个不同属性 / 36 处引用,别无分号。
- **耦合很窄而非很宽**:24 个属性各仅被引用 **1 次**(每条策略 lambda 独立、可逐条迁移、零交叉);仅 3 个 >1 引用——`_run_llm_driven_exploration`(4,多分支兜底,需保留为显式注入字段)、`state`(6,**全是 `ctx.state or ctx.dispatcher.state` 兜底**)、`_structured_followup_value`(3,集中一个 precondition)。这把 P3b 从「40+ lambda 纠缠重写」降级为「逐条机械迁移」。
- **契约已半显式化**:`ChainContext`(`strategy_registry.py:13`,`@dataclass(slots=True)`)早有显式字段 `state/runtime/capability_registry/strategy_memory/exploitation_mode`,但 `dispatcher: Any` 仍在、且后 4 个字段在 registry 内零引用(挂着没接线)。P3b 本质=把已备好的显式字段接上线、再删 `dispatcher` 透传。
- **风险定性仍成立**:P3b 改的是调用路径(`ctx.dispatcher._run_X`→`ctx.services._run_X`),**非 P5 的字节级搬迁**,「单测绿 ≠ 解题行为不变」仍需警惕;故每刀除单测外建议配相关 live 题回放(eval harness 已建)。**唯一例外是 state 通道收口**(下条),因可证 `ctx.state` 与 `ctx.dispatcher.state` 在生产中恒为同一对象、且 `_DummyDispatcher` 无 `.state`,纯推理即可证零行为变更,无需 live 回放。
- **渐进切法**:①state 通道收口(零风险破冰)→ ②`_structured_followup_*`/`_challenge_context` 等零散状态查询下沉为显式字段 → ③主体 22 个 `ctx.dispatcher._run_*/_attempt_*` lambda 引入显式 `services` 字段逐条迁(`_run_llm_driven_exploration` 优先)→ ④删四个死字段 + 最后摘 `dispatcher: Any`。

#### P3b 第二刀完成(2026-06-20,state 通道收口)
按渐进切法第①步落地,**消除 `ctx.dispatcher.state` 这条最宽的非方法透传**(测绘里 `state` 6 引用全是兜底)。`strategy_registry.py` 里 5 处 precondition 的 state 读取(`_hash_guarded`/`_render_param`/`_xss_admin_bot`/`_ssti_probe_ran`/`_ssti_engine_identified`)统一改为只读显式 `context.state`:A 型(`ctx.state or ctx.dispatcher.state` 兜底)删兜底、B 型(`_xss_admin_bot` 原**只**读 `dispatcher.state`)改读 `ctx.state`、C 型(SSTI 两处 `state or 兜底`)删兜底;变量 `dispatcher_state`→`context_state` 去误导命名。**零行为变更的证明(无需 live 回放)**:生产中 `_strategy_context`(`ctf_dispatcher.py:1718`,全仓唯一生产构造点)恒设 `state=self.state`,故 `ctx.state` 与 `ctx.dispatcher.state` 恒为同一对象,兜底分支永不可能产出不同值——是可证死代码。**配套测试**:`test_ctf_strategy_registry.py` 两个用例此前给 `_DummyDispatcher` 实例**动态挂 `.state`** 却不向 context 传 `state=`(靠被删的兜底),补 `state=dispatcher.state`(同引用,后续 `observations.append` 仍可见)使其忠实于生产构造方式——这是"删被旁路的兼容路径需同步修不忠实测试"的标准伴随,非行为回退。`strategy_registry.py` 的 `ctx.dispatcher.state` 读取 5→**0**;剩余 dispatcher 透传 = 22 个 execute lambda(`_run_*/_attempt_*`,留主体刀)+ `_structured_followup_*`(119-122)/`_challenge_context`(267)零散状态查询(留第②刀)。**全套件 489 passed 零回归。**

#### P3b 第三刀完成(2026-06-20,零散状态查询下沉)
按渐进切法第②步落地,**消除两处经 dispatcher 的零散状态查询**。给 `ChainContext` 加两个显式字段 `ingress_handoff: dict` / `challenge_context: dict`(`field(default_factory=dict)`),由唯一生产构造点 `_strategy_context` 填充(`ingress_handoff=self._ingress_handoff if dict else {}`、`challenge_context=self._challenge_context if dict else {}`,镜像 dispatcher 同名属性)。两个 precondition 改读显式字段:`_hint_chain_precondition` 把 4 处 `dispatcher._structured_followup_next_action()/_value(key)` 改为 `ctx.ingress_handoff.get("nextAction"/key)`(等价:这俩 helper 本就只读 `self._ingress_handoff.get(...)`),并删去不再需要的 `dispatcher` 句柄取用 + `try/except`(dict.get 不抛);`_artifact_forensics_precondition` 把 `getattr(getattr(ctx,"dispatcher",None),"_challenge_context",{})` 改为 `ctx.challenge_context`。**生产等价可证**:工厂字段与 helper 读的是同一 dict、`None→{}` 兜底一致。**test-neutral**:`_DummyDispatcher` 既无 `_ingress_handoff` 也无 `_challenge_context`,新字段默认 `{}` → precondition 取空,与改前一致(改前 hint_chain 调不存在的方法→`except`→False、artifact_forensics `getattr→{}`)。至此 `strategy_registry.py` 内**经 dispatcher 的状态查询全部清零**;剩余透传仅 23 处 `ctx.dispatcher._run_*/_attempt_*` 点访问(22 个 execute lambda + `_run_llm_driven_exploration` 别名),即第③步主体刀。**全套件 489 passed 零回归。**

#### P3b 主体刀完成(2026-06-20,execute lambda 透传破除)
渐进切法第③步(主体)落地,**消除 `strategy_registry.py` 全部 22 处 `ctx.dispatcher._run_*/_attempt_*` 透传**。给 `ChainContext` 加显式服务面字段 `services: Any | None = None`,由唯一生产构造点 `_strategy_context` 设 `services=self`(过渡期与 `dispatcher` 同一对象)。所有策略 execute lambda 与三个 probe 函数(`_execute_jwt_probe`/`_execute_graphql_probe`/`_execute_nosql_probe`)统一改走 `ctx.services._X(...)`:19 个同构 execute lambda 一次 `replace_all`(`execute=lambda ctx: ctx.dispatcher.` → `ctx.services.`,不误伤注释里的字面 `ctx.dispatcher._run_X`),3 个 probe 体的 `dispatcher = context.dispatcher` 改 `context.services`,`_run_llm_driven_exploration` fallback lambda 一并迁移。至此文件内除注释外**零 `ctx.dispatcher` 引用**。**生产等价可证(非真·改路径)**:`services` 恒 `=self`,与 `dispatcher` 同一对象、同一 MRO 方法解析,`ctx.services._run_X` 与 `ctx.dispatcher._run_X` 字节级等效——故本过渡刀**不触发 live 回放门槛**(该门槛只对未来「`services` 收窄成不同对象」的真·改路径生效),全 agents 单测即足够门禁。**夹具修正**:唯一调用 `.execute()` 的测试(`test_strategy_registry_executes_php_unserialize_strategy`)补 `services=dispatcher`(同一 `_DummyDispatcher` 实例,镜像生产);其余 10 处只测 precondition 的 `StrategyContext` 构造无需 `services`。**全套件 489 passed 零回归。** 剩余收尾(第④刀,待评估):删 4 个零引用死字段 `runtime/capability_registry/strategy_memory/exploitation_mode`,并决定 `services` 是否收窄为 narrowed Protocol、`dispatcher: Any` 能否摘除(若收窄则升级为真·改路径,需 live 回放兜底)。

#### P3b 第④刀完成(2026-06-20,收尾:摘 dispatcher + 删死字段,refactor `0308774` + docs `057f5c2`)
渐进切法第④步(收尾)落地,**摘除 ChainContext 过渡用的 `dispatcher: Any` 字段,并删 4 个自始零引用的死字段** `runtime/capability_registry/strategy_memory/exploitation_mode`。主体刀把 `strategy_registry.py` 全部 execute 路径迁到 `ctx.services` 后,`dispatcher` 字段在生产代码内**零读取**(全仓 grep `ctx.dispatcher`/`context.dispatcher` 仅余 ChainContext 内注释字面),仅测试构造/断言尚在引用;4 个死字段则自加入起就在 registry/chains 内零引用(本刀前再次全仓复扫确认)。**改动**:① `ChainContext` 删这 5 个字段,字段集由 13 收敛到 **8**(`services/target/page_features/hint/extras/state/ingress_handoff/challenge_context`);② 工厂 `_strategy_context` 去掉 `dispatcher=self` 与 4 个死字段 kwarg 赋值,只留 `services=self` 等存活字段;③ 测试同步——`test_ctf_dispatcher` 的 explicit-fields 用例断言收敛到 `services`/`state`,`test_ctf_strategy_registry` 11 处直接构造把 `dispatcher=`/`dispatcher=None` 改 `services=`(php execute 用例去掉冗余 `dispatcher=`、保留 `services=dispatcher`),原专测 4 死字段的 `test_strategy_context_exposes_chain_context_fields` 改测存活字段 `services/state/ingress_handoff/challenge_context`(含 `None→{}` 默认)。**风险定性**:本刀**不收窄** `services`(仍 `=self`、类型 `Any`),故是纯结构清理、可推理证零行为变更,**非主体刀注明的「真·改路径」**(那条 live-回放门槛只对未来把 `services` 收成不同对象/Protocol 才生效);全 agents 单测 + eval 回放即足够门禁。至此 **P3b(关节 B `ChainContext` 破上帝对象)收口**:`strategy_registry.py` 内零 dispatcher 透传、`ChainContext` 仅余显式契约字段。**全套件 493 passed(489 agents + 4 eval 回放)零回归。**

#### 卡 E 收口:benchmark backup_node_app:zip 验收链转绿(2026-06-20,fix `ed8cdb4` + docs)
非 P 线架构刀,属「能力已实现却被静默禁用」族 bug 修复(对照 `根因characterization_2026-06-16_V1.md` 方法论:先核触发条件再改)。**根因**:`artifact_forensics.py` 在 P5 抽包时**顶层漏 `import json`**;`_analyze_attachment_artifact` 里 `json.loads(text.splitlines()[-1])` 抛 `NameError`,被裸 `except Exception` 吞掉后于 except 分支提前 return——导致 **HTTP 下载与本地 `file://` artifactPaths 两条路径**的 `ctf_artifact_forensics` note 与 `artifact_forensics_summary` 观测**从不落地**(`json` 在嵌入式取证脚本字符串里另有 import、在子进程内执行,恰好掩盖了模块级缺失)。分类器 `_has_source_only_artifact_forensics_signal` 因缺 `artifact_forensics_summary` 观测把 zip 变体判成 `honest_no_flag`。**修复**=补一行 `import json`,让既有落地代码真正执行,与 HTTP 路径语义完全一致;backup.zip 内无 flag,不引入假 verified。**验证**:① benchmark `local_backup_node_app_zip` 转绿(matched=True/`candidate_only_honesty`);② sibling 集成 15/15 绿(`test_backup_node_app_candidate_eval` 此前正卡在 `ctf_artifact_forensics` note 缺失,现通过);③ 隔离重跑全部 14 例 dispatcher 验收链——**基线 13/14**(`test_ctf_dispatcher_acceptance_misc_attachment_artifact_forensics` 因同一 NameError 取不到 forensics_record 而 `StopIteration` 预存失败)**→ 修复后 14/14**(顺带修好该预存失败,零新增回归);全量长跑里那 13 例的偶发失败经隔离复跑证实为端口/资源竞争抖动(基线隔离同样通过),非本改动所致。

#### P5 命名/文档线尾巴清理(2026-06-21,卡 F/G/H)
卡 B(P5 命名/文档线)收口后浮出的三条自包含尾巴,本批清理完毕:
- **卡 F**(`837f0d2`):M6 `apply_turbo` 经核为零调用点死代码(全仓仅定义/导出/文档、生产侧 0 调用),删除;保留 `_wrap_tool` registry;新增 deadcode 守卫测试。M6 加速面收敛为 `/turbo` 命令。
- **卡 G**(`b52cb0c`):`session/initializer.py` CPA 钩子门控统一——M1/M3/M4/M6 走 `is_mN_enabled()`;M2/M5 因 `is_mN_enabled` 含 `_initialized` 前置(用于 init 前门控会自锁),刻意保留直读 env,属设计决策。新增 `test_cpa_hook_gating.py` 守卫(27 项)。
- **卡 H**(`399f262`):easy_tornado 观测命名复核,确认根因文档 §1.1 的"未记录 render_ssti_response"系 `0957d94` 之前过时快照,生产与测试现已自洽(纯 docs 复核,未触生产)。
三卡文件锁互斥并行执行,均零越界、抽验全绿、工作树无残留。

#### P3b L1 完成(2026-06-21,services 收窄为 StrategyServices Protocol,refactor `b73b6bf`)
落地主体刀/第④刀明确预告的「另一张高风险卡」——把 `ChainContext.services` 的静态类型从 `Any` 收窄为显式 `StrategyServices` Protocol(`strategy_registry.py` 顶部新增,带 `@runtime_checkable`),刻画策略 execute lambda 经 `ctx.services.<method>` **无条件调用**的 **20 个 dispatcher 必调成员**(全部 `async ... -> _ChainOutcome`:`_run_llm_driven_exploration` / `_attempt_auth_form_sqli` / `_attempt_generic_param_sqli` / `_attempt_generic_param_cmdi` / `_attempt_generic_param_ssrf` / `_attempt_stored_xss_chain` / `_run_unicode_numeric_form_bypass_strategy` / `_run_contact_report_chain_strategy` / `_run_artifact_forensics_strategy` / `_run_backup_source_leak_strategy` / `_attempt_php_unserialize_chain` / `_run_hint_chain_followup_strategy` / `_run_file_read_endpoint_strategy` / `_run_hash_guarded_file_read_strategy` / `_run_hash_reconstruction_attack_strategy` / `_run_render_parameter_ssti_strategy` / `_run_tornado_ssti_strategy` / `_run_ssti_probe_strategy` / `_run_ssti_identify_strategy` / `_run_ssti_exploit_strategy`)。`_ChainOutcome` 从依赖无环的 `chains/base` 导入(`chains/__init__` 为空、`base` 仅 import dataclasses,无循环 import)。**3 守卫裁决(本卡核心决策,采 option b)**:`_run_jwt_manipulation_strategy` / `_run_graphql_introspection_strategy` / `_run_nosql_injection_strategy` 这三个 hasattr 守卫成员**刻意留在 Protocol 外**——它们带 LLM fallback、语义上可缺失,纳入「必有」契约会与运行期 optional 语义矛盾,故保留 `hasattr` 探测、走 duck-typing,并在三个 probe 函数处加注释标注(把 optional 写成 required 会失真)。**风险定性与门禁**:这是主体刀注明的「`services` 收窄成不同对象/Protocol」真·改路径,本应受 live 回放门槛约束;但唯一生产构造点 `_strategy_context` 仍设 `services=self`——运行期同一 `CTFTaskDispatcher` 对象、同一 MRO 方法解析,**本刀仅静态类型收窄、零行为变化**,live 回放门禁(`test_replay_harness.py` 5 passed)如期兜底零差。**边界**:仅动 `strategy_registry.py` + `tests/unit/agents/test_ctf_dispatcher.py`(`ctf_dispatcher.py` 本次未触——`services=self` 早于第④刀已就位,L1 只需收类型标注),coordinator/recon/chains/eval 均未越界。**门禁**:`tests/unit/agents/` **489 passed / 0 failed**、`tests/eval/test_replay_harness.py` **5 passed / 0 failed**。至此 **P3b(关节 B `ChainContext` 破上帝对象)从「结构破除」升级到「显式类型契约」彻底收口**:`services` 面已是有名有据的 20 成员 Protocol + 3 守卫 duck-typed 旁路,`dispatcher: Any` 透传彻底退场。

#### P3b L2a 完成(2026-06-21,coordinator 的 `dispatcher: Any` 收窄为 CoordinatorDispatcherServices Protocol,refactor 见 §8)
把 L1 的同一手法用到 **关节 B 延伸的 coordinator 层**——`coordinator.py` 顶部新增 `@runtime_checkable class CoordinatorDispatcherServices(Protocol)`,刻画 `CTFCoordinator` 把 dispatcher 当**方法参数**逐个传入时所**无条件访问**(bare `dispatcher.X`,非 `getattr`/`hasattr` 守卫)的依赖面,共 **39 个成员**:**22 个必调方法** ——audit/ledger/checkpoint 6(`_setup_session_ledger` / `_setup_artifact_registry` / `_setup_checkpoint_store` / `_record_session_event` / `_write_checkpoint` / `_record_recovery_decision`,AuditInfraMixin)、dispatcher 本体 7(`_apply_submit_profile` / `_start_failover_monitor_if_available` / `_select_primary_strategy` / `_primary_capability_for_chain` / `_select_hypothesis_for_chain` / `_emit` / `_finalize_solve_result`)、flag/verify 3(`_observe_flag` / `_build_already_solved_reason` / `_load_rejected_flags`)、recon 3(`_phase_recon` / `_explore_agenda_items` / `_snapshot_platform_context`)、notes 2(`_store_missing_tools` / `_store_retrospective`)、progress 1(`_derive_progress_delta`)——加 **17 个必有属性/引擎句柄**(`state: CTFState | None`、`strategy_memory: Any | None`、`reasoning_layer` / `capability_registry` / `tool_guard` / `recovery_controller` / `hypothesis_engine` 五句柄标 `Any`,以及 10 个 run-scratch 字段 `_challenge_context` / `_notes_log` / `_ledger_run_id` / `_current_fingerprint` / `_memory_match_ids` / `_pending_wrong_flag_feedback` / `_exhausted_visit_url_targets` / `_restored_resume_checkpoint_id` / `_local_challenge_artifacts_loaded` / `_registered_local_source_hints_loaded`)。**主控裁决(本卡唯一高判断点)**:① 采**单个大 Protocol**(与 L1 `StrategyServices` 同构,因 `_apply_*_contract` 方法横跨多簇,分组会逼出继承全部分组的总 Protocol、净增间接层);② `strategy_memory` 虽仅经 `getattr` + `is None` 早返回访问,但属核心依赖,**纳入 Protocol 标 `Any | None`**(依赖面完整性 > 严格访问形态)。**7 个 optional/duck-typed 守卫成员刻意留在 Protocol 外**(比照 L1 option b):`_ingress_handoff`(getattr attr,可缺失)+ `_restore_context` / `_ingest_local_challenge_artifacts` / `_ingest_registered_local_source_hints` / `_align_platform_challenge` / `_extract_flag` / `_record_ingress_handoff_observations`(全 getattr + `callable()` 守卫),它们语义可缺失,保留 duck-typing 并在 Protocol docstring 标注。**裁决保留 4 处 `Any`**:`_resolve_recon_target_from_structured_handoff` / `_resolve_control_decision_from_structured_handoff` 两个模块级 helper 参数(只摸 OUT 成员 `_ingress_handoff`,收窄无益)+ `RunContext.__init__` 参数 / `RunContext.dispatcher` property(卡明令不改 RunContext;`_run_solve_loop` 经 `ctx.dispatcher`(Any 逃生口)调用,不入 Protocol)。**收窄处**:23 处合约方法 `dispatcher: Any` → `dispatcher: CoordinatorDispatcherServices`(8 空格缩进唯一锚,replace_all 精确命中,不误伤 4-空格模块函数与内联 RunContext)。**风险定性与门禁**:属「真·改路径」族(改类型契约),但注入对象不变(生产仍无参 `CTFCoordinator()` + 逐参数传真实 `CTFTaskDispatcher`)、Protocol 静态标注**运行期不强制**,纯静态收窄、零行为变化;调用体 163 处 `dispatcher.` 访问点一行未改。**边界**:仅动 `coordinator.py`(测试 fake dispatcher 靠 duck-typing 满足实际调用子集,不受静态标注影响,**零改动**),ctf_dispatcher/executor mixin/chains/eval/RunContext 实质逻辑均未越界。**门禁**:`tests/unit/agents/` **489 passed / 0 failed**、`tests/eval/test_replay_harness.py` **5 passed / 0 failed**(live 回放零差兜底)。至此**关节 B 的 coordinator↔dispatcher 隐式依赖面亦显式化成契约**,与 L1 的 strategy 层 `StrategyServices` 对称收口。

#### P3b L2b 完成(2026-06-21,RunContext 的 dispatcher 收窄到 Protocol·闭合 L2a 缺口,refactor `6f43a87`)
L2a 把 23 处合约方法形参收窄为 `CoordinatorDispatcherServices`,但 `execute()`(L1707)喂进这些形参的是 `RunContext(dispatcher)` 透明代理(`__init__(dispatcher: Any)`、`dispatcher` property `-> Any`、`__getattr__`/`__setattr__` 全代理)——**契约恰在包装器这一环仍是 `Any`**(L2a「不碰 RunContext」非目标点名延后的 L2b+)。本卡收窄之,闭合该缺口。**Phase 0 取证**:`RunContext` 仅定义/用于 `coordinator.py`,构造点唯一 = `execute()` L1707,`ctx` 流入全部 23 个合约形参;`ctx.dispatcher.X` 逃生口访问**仅 1 处** = L1790 `ctx.dispatcher._run_solve_loop(...)`。**主控裁决(3 点)**:① **纳入 `_run_solve_loop`**(`async`、真实签名 `*, target/hint/page_features/detected_type/chain_order/result`,返回标 `Any`——`SolveResult` 在 ctf_dispatcher,coordinator 顶层 import 会循环)——这是把 `RunContext.dispatcher` property 从 `Any` 收窄的**前置耦合**(否则 L1790 静态断裂),Protocol **39→40 成员**;② **RunContext 不显式继承 Protocol**——它定义了返回 `Any` 的 `__getattr__`,类型检查器据此视其结构化满足任意 Protocol,最小改动、`class` 头不动;③ **2 个模块级 helper 形参保留 `Any`**(`_resolve_recon_target_from_structured_handoff` / `_resolve_control_decision_from_structured_handoff`,只经 `getattr` 摸 OUT 成员 `_ingress_handoff`,收窄零收益且语义不诚实——会暗示该成员在契约内)。**落地**:`RunContext.__init__(dispatcher: CoordinatorDispatcherServices)` + `dispatcher` property `-> CoordinatorDispatcherServices`;`__getattr__`/`__setattr__` 运行期行为一行未改,仍是透明代理。**风险定性**:真·改路径(改类型契约),但 `from __future__ import annotations` 在顶,注解运行期不求值、零行为变化;注入对象不变。**边界**:仅动 `coordinator.py`,测试零改动。**门禁**:`tests/unit/agents/` **489 passed / 0 failed**、`tests/eval/test_replay_harness.py` **5 passed / 0 failed**(live 回放零差兜底)。至此 **coordinator 自身调用路径的 `Any` 缺口全闭合**(`coordinator.py` 内仅余 2 处裁决保留的模块级 helper `Any`);RunContext 不再是契约里的类型黑洞,L2a+L2b 合并完成「关节 B coordinator 层 dispatcher 依赖面显式化」。

#### P3b L2c 完成(2026-06-21,补 CoordinatorDispatcherServices 生产侧 conformance 门禁,test `b200123`)
L1/L2a/L2b 把契约建在**消费侧**(形参 / 包装器视图),但没有静态/动态证据保证**生产侧**真实 `CTFTaskDispatcher` 确实供齐契约成员——某 mixin 方法被改名/删除时,只有运行期才炸。L1 其实早已留了对称范式:`test_ctf_dispatcher.py:73` 的 `assert isinstance(dispatcher, StrategyServices)`(CI 强制)。本卡为 L2a/L2b 的 `CoordinatorDispatcherServices`(40 成员)补上**同款生产侧 conformance 断言**,闭合供给侧验证缺口。**Phase 0 取证**:① `mypy` 未安装、CI 的 lint job 只跑 `ruff`+`black`(均 `continue-on-error`)→ **静态检查器零强制力,pytest 是 CI 唯一强制门**;② `CTFTaskDispatcher.__init__`(L299–328)初始化全部 17 个数据成员,23 方法 + `_run_solve_loop` 由 MRO 上 mixin 提供 → 新构造实例即结构性满足 `isinstance`;③ 用 `isinstance`(非 `issubclass`)→ `runtime_checkable` 的 data-member 检查跨 Python 3.10–3.12 一致、不触 `issubclass` 对 non-method-member 的 `TypeError`。**无高判断点**:设计由 L1 对称性唯一确定,不设裁决门。**落地**:`test_ctf_dispatcher.py` 加 `CoordinatorDispatcherServices` import + 专用 test `test_dispatcher_conforms_to_coordinator_dispatcher_services_protocol`(构造真实 dispatcher、断言 `isinstance`)。**边界**:仅动 `tests/unit/agents/test_ctf_dispatcher.py`,**零生产代码改动**。**门禁**:`tests/unit/agents/` **490 passed / 0 failed**(489+本卡新增 1)、`tests/eval/test_replay_harness.py` **5 passed / 0 failed**。至此 **L2 线(关节 B coordinator 层 dispatcher 依赖面契约化)消费侧+生产侧双向闭合**,与 L1 的 strategy 层完全对称:消费侧形参/包装器收窄(L2a/L2b)+ 生产侧 conformance 门禁(L2c)。

#### N1–N4 pa_agent 外死代码/bug 清理批次(2026-06-21,S 扫描衍生,4 对话并行)
P3b/L2 主线之外,经 S 只读扫描(范围 tools/llm/knowledge/runtime/mcp/cpa_modules/config/interface/playbooks/workspaces,排除 pa_agent/eval/tui 命令分发)挖出 4 个自包含切片,文件锁两两不重叠、与 L2(coordinator)/eval 零交集,4 对话并行落地,主控逐张审核(边界 + 就近测试)。**N1**(`6afd433`)删 llm/utils.py 5 死函数 + count_tokens/truncate_to_tokens(同步 llm/__init__ import/__all__)+ 修 count_tokens 恒等分支(`if "gpt-4"/"gpt-3.5"` 两分支同赋 `cl100k_base`、`model` 实际被忽略),parse_llm_json 保留;**N2**(`6e2126d`)删 m6_turbo 5 个零调用顶层便捷包装(lazy_get/lazy_wrap/install_lazy_hook/get_memory_mb/quick_cleanup),保留 LazyLoader/MemoryOptimizer 类;**N3**(`27b9092`)修 base_playbook.py 在非 `@dataclass` 上误用 `field()` 致 phases 默认值为 `Field` 对象的潜伏 bug(未覆写子类 `get_task()` 会迭代 `Field` 崩)+ 新增守卫测试;**N4**(`8e77449`)删 knowledge/retrospective.py 死代码 `_safe_list`。4 张均零越界、未碰 backlog/ADR(文档主控串行收口),就近测试 96+4 passed、import 冒烟 EXIT=0、零回归。**判断题两张(C5/C6,主控开 background agent 自托管并行)亦落地**:C5(`4884979`)token_tracker.set_data_file 经查是测试专用注入点(test_token_tracker.py fixture)→保留+docstring 标注非删;C6(`b88bb81`)m5 get_vote_status 经查是预留对外契约接口(兄弟方法被 swarm_commands 消费、语义对称)→保留+补 4 契约测试坐定行为。**两张均"确证有用即保留+留证、不为清理而删"的保守落点**,零生产删除/C6 零生产改动。

#### S 扫描②批次(2026-06-21,3 真 bug + 1 死代码 + 1 docstring,4 background agent 并行)
第二轮 S 只读扫描挖出 5 个互斥切片,主控开 `run_in_background` agent 自托管并行(非手动开对话)+ 逐张审核。**S1+S2**(`6de4d1d`)修 m5/pheromone_router.py 两 bug:heapq 入堆元组加 `(-score,idx,prio)` tie-breaker 消除同 final_score 时比较不可比较 TaskPriority 的 TypeError 崩溃;新增 `is_active_above(threshold)` 让 `get_active_trails` 吃 router 的 `self._threshold`(修写死 0.1 无视构造 threshold);**S3**(`0e5ea9f`)删 interface/utils.py 4 个零调用展示函数 + 级联 `truncate_output`;**S4**(`0c7f0cb`)cost_tracker.get_recent_logs docstring "从新到旧"→"从旧到新"对齐实现(grep 零调用方、零行为变更);**S5**(内容落 `9704e20`)修 knowledge/indexer.py 大写 `.JSON` 因 `:161` 大小写敏感比较误走 YAML 分支(改 `.lower()==".json"`)。各自就近测试全绿(11/267/25/59 passed)、零越界。**⚠ 并发竞争遗留**:4 agent 并发提交 staged 竞争致 `9704e20` 装 S5 内容却错挂 S4 的「docs(m1)」message;S4 已 reset+pathspec 重做干净的 `0c7f0cb`。两份改动均完整无丢失/无重复,仅 9704e20 subject 名实不符;按"不改写历史"铁律(活跃并发期 rebase 风险高)保留原样,**真实映射:S5=`9704e20`、S4=`0c7f0cb`**。

#### L3「真断透传」线 L3a 完成(2026-06-22,RunContext 字段分流机制·破冰刀,refactor `3bd372e`)
L2(L2a/b/c)把 coordinator↔dispatcher 契约**显式化**成 `CoordinatorDispatcherServices` Protocol,但运行期 coordinator 仍把整个 dispatcher 当参数透传。**L3 线**目标=让 coordinator 经 `RunContext` 承载 run-state/executor、dispatcher 缩成 façade。Plan agent 测绘关键发现:`RunContext`(coordinator.py)**已全程接通 execute() 主干**(`ctx=RunContext(dispatcher)`,18 个 `_apply_*` 收 ctx,L2b 落地),但仍纯透明代理、承载量 0;其 `__setattr__` 全代理(`ctx.foo=X` 落 dispatcher)是后续一切真承载的**硬闸门**。**主控裁决**:采"候选 A(建机制不承载)"作破冰第一刀,否决"executor 对象化先行"(后者牵涉面大、需 live 回放,是平行主线,应从最闭合的 ProgressTracker/AuditInfra 起手而非 Recon)。**L3a 落地**:给 RunContext 加 `_OWN_FIELDS=frozenset()` 空白名单 + object.__* 分流(`__setattr__` 白名单内走 `object.__setattr__` 否则代理;`__getattr__` 对内部名 `_dispatcher`/`_OWN_FIELDS` 短路抛 AttributeError 防代理递归、白名单未设值不代理防影子读穿、其余代理),**白名单空→行为等价纯透明代理、可推理证零行为**;把"机制"与"承载"解耦成两刀。**关键发现(纠正 DoD)**:Py3.12 `runtime_checkable` 的 isinstance 走 `getattr_static`(忽略 `__getattr__`)→透明代理 ctx 本身不满足 Protocol,L3a 前后一致非回归,真实满足契约的是 `ctx.dispatcher` 真身;测试改坐实"L3a 不改 isinstance 行为"而非假前提。**边界**:仅 coordinator.py RunContext 类(+28)+ 新 `test_ctf_run_context.py`(10 测试)。**门禁**:`tests/unit/agents/` 500 passed、`test_replay_harness.py` 5 passed。**L3 渐进路线**:L3a(分流机制·破冰·已完)→ L3b(RunContext 真承载 `state`·共享引用·需 live 回放)→ L3c+(executor 对象化:ProgressTracker→AuditInfra→note_store/flag_observer→recon/llm/jwt)。L3b 起风险升至中/高,**回主控裁决再派**。

#### L3「真断透传」线 L3b 完成(2026-06-22,ProgressTracker 对象化·执行体对象化第一刀·裁决换序,refactor `35d7649`)
L3a 为 L3b 留的题面是"RunContext 真承载 `state`"。**主控派 Plan 测绘后裁决换序**:测绘坐实 `state` 是**最难安全承载**的字段——唯一运行期 rebind 在 `ctf_dispatcher.py:1680`(`_restore_context` resume 路径,`CTFState.from_snapshot()` 返**新身份**对象),故 eager 引用承载(切法 B)会在 resume 后 ctx 旧引用与 dispatcher 新 state **分叉失同步**,而 dispatcher 不持 ctx 引用、回写须走反向通道(复杂度高、必跑 live 回放)。**裁决:`state` 承载延后,先在最闭合·零扇出的 `ProgressTracker` 上验证「执行体对象化」范式**(原路线 L3c 的内容提前为 L3b)。第二份测绘确认 ProgressTracker 真零扇出:`progress_tracker.py` 的 `ProgressTrackerMixin` 仅 2 方法、只读 `self.state`、零 notifier/config/兄弟 mixin 依赖,仅 2 调用站(`ctf_dispatcher.py:449` self 调、`coordinator.py:1467` 裸 dispatcher 形参调)。**切法选型**:采 **A(独立 class + dispatcher 持实例 + mixin 退化委派壳·不动 MRO)**;否决 B(摘 MRO + ctx 承载,但 `dispatcher.py:449` 无 ctx 句柄→要么退化成 A、要么透传 ctx 进 solve-loop 造新扇出,违"最闭合"初衷)与 C(收益小)。**L3b 落地**:新增独立 `ProgressTracker` 类——**无 `__init__`、零 eager 持 state 引用(规避 rebind 坑)**,`snapshot_flag_counts(self, state)`/`derive_progress_delta(self, state, before_state, chain_outcome=None)` 显式收 state、无继承、无 dispatcher-only 依赖;`ProgressTrackerMixin` 两方法退化为 `return self._progress.<m>(self.state, …)` 委派壳(`__module__` 锚定测试仍过);`ctf_dispatcher.py:73` import、`:301` `self._progress = ProgressTracker()`;**MRO `:260` 未删、调用站 449/1467 未改、coordinator.py 不在 diff**,行为等价;`:76-78` `chain_outcome.progress` 短路 → `"none"`(非 `"rejected"`)bug-fix 逐字保留。**边界**:`progress_tracker.py` + `ctf_dispatcher.py` + `test_ctf_progress_tracker.py`(3 文件 +140/−24);纯计数·零 I/O→**不跑 live 回放**。**门禁**:`tests/unit/agents` 425 passed/5 skipped(零回归)、新测文件 6 passed。独立坐实 agent 复核 1–7 全通过。**后续**:摘 MRO + ctx `_progress` 承载 + 把 1467 路径切 ctx(承载只对经 ctx 的调用站生效)→ 独立后续卡;`state` 真承载(rebind 同步纳入承载契约或 property 写穿切法 C)仍延后。

#### L3「真断透传」线 L3c 完成(2026-06-22,flag_observer 对象化·执行体对象化第二刀,refactor `563d525`)
延续 L3b 验证的"切法 A:独立 class + dispatcher 持实例 + mixin 退化委派壳·不动 MRO"套路,推进 executor 对象化。**主控派 Plan 测绘比选 3 候选**(AuditInfra/note_store/flag_observer)。**裁决取 flag_observer**:三者里唯一**零反向依赖底座**——`_observe_flag` 的 35 处调用全是消费者(chains/executors/coordinator)调它、**无任何兄弟 mixin 调回**,对象化不破单向依赖;本体无直接 I/O(落盘经 `_store_note` 委派仍留壳)。**AuditInfra 排除**(166 处反向调用的被依赖底座 + 三类磁盘 store 运行期 rebind,对象化会掀翻单向依赖图且 eager-hold 必踩 None 快照);**note_store 推 L3d**(60 处反向调用 + 直写 `loot/notes.json`,做它要先解决落盘 replay 隔离)。**L3c 落地**:新增 `FlagObserver` 类(无 `__init__`、零 eager 持 state/context),`observe_flag` 把 `state`/`_active_hypothesis_context`/`_active_strategy_context` 作 **per-call 传值**(每轮+resume rebind 不能 eager 持)、`verifier` + 4 个兄弟方法(`_store_note`/`_record_session_event`/`_hydrate_flag_proof`/`_record_wrong_flag_feedback`)作 **per-call 注入 bound method**(壳里现取跟随 MRO);mixin `_observe_flag` 保留原签名退化委派壳(35 调用站零改),`__module__` 仍在 flag_observer;`ctf_dispatcher.__init__` 持 `self._flag_observer=FlagObserver()`;**MRO/调用站/coordinator 零改**。否决切法 B(`dispatcher` 调用站无 ctx 句柄)。**与 L3b 关键差异**:flag_observer 在 35 条真实 exploit 链热路径上、触发 verifier + 落盘链路,故 DoD **跑 live replay 兜底**(L3b 纯计数才免),卡升为独占 pa_agent。**门禁**:`tests/unit/agents` 515 passed(零回归)+ `test_replay_harness.py` 5 passed(live 回放零差)+ 新 detached 单测(脱离 dispatcher 跑 4 分支/None 守卫/rejected 条件/per-call 不 eager)。**过程**:执行 agent 改完撞服务端 529 过载(连带安全分类器不可用→主控写操作全被挡),改动滞留工作树;主控核 `--stat` 形态后经用户 `!` 自助跑门禁+提交落地,零丢失(沉淀为通用应对:被挡时备 `!` 命令交用户执行)。**后续**:L3d note_store→AuditInfra 最后;承载收尾(摘 MRO + ctx `_flag_observer`/`_progress` 承载 + 调用站切 ctx)与 `state` 真承载(rebind 坑)仍延后。

#### L3「真断透传」线 L3d 完成(2026-06-22,note_store 对象化·执行体对象化第三刀,refactor `2de4d1f`)
延续切法 A 第三刀。**主控派 Plan 测绘**:`NoteStoreMixin` 6 方法(`_store_note` 核心 + `_store_secret_note`/`_store_missing_tools`/`_store_retrospective` + 两个纯函数 `_derive_artifact_producer`/`_derive_artifact_category`),对象化适配度评 4/5。测绘澄清三个"额外变量"两个非风险:① **落盘非风险**——写盘不在 note_store,它只调 `notes_tool` 单例,落盘逻辑/路径全在范围外,且 replay 已有现成 `set_notes_file` tmp 隔离(`eval/replay.py`);② **约 40 处反向调用非风险**——全 `self._store*`/`dispatcher._store*`,委派壳保原名原签名后零改,coordinator 的 Protocol 桩(`_store_missing_tools`/`_store_retrospective`)只要壳签名逐字一致也无需动。**唯一真坑 = `_notes_log` 归属**:它是 dispatcher 持有的可变 `list[str]`(`ctf_dispatcher.py:299`),`:574 result.notes = list(self._notes_log)` 直接读它——**必须留 dispatcher、委派壳 per-call 传 list 引用**(靠 list 可变引用语义原地 `.append`,字节级等价于原 `self._notes_log.append`),**绝不搬进 NoteStore 实例持有**(否则断 `result.notes` 取数=非零行为)。**L3d 落地**:`NoteStore` stateless 类(6 方法逐字搬入,`_derive_*`→`derive_*` 纯函数随迁、`_store_note` 内部改调 `self.derive_*`),`state`/`runtime`/4 个兄弟方法(`_record_session_event`/`_register_artifact_record`/`_select_hypothesis_for_chain`/`_emit`)/`reasoning_layer` 全 **per-call 传值/注入 bound method**,`_notes_log` per-call 传引用;`NoteStoreMixin` 6 方法保原名原签名退委派壳(`__module__` 锚定 note_store);`ctf_dispatcher.__init__` 持 `self._note_store=NoteStore()`;**MRO/调用站/coordinator/Protocol 桩零改**。**门禁**:`tests/unit/agents` 519 passed(零回归)+ `test_replay_harness.py` 5 passed(落盘隔离零差)+ 新 detached 单测(脱离 dispatcher + `set_notes_file(tmp)` + 独立 list 充 notes_log,专门断言 NoteStore 无 `_notes_log` 属性证未自持)——**主控亲跑门禁独立印证**(同 519 + 5)。**后续**:AuditInfra 底座(166 反向调用 + 三类磁盘 store 运行期 rebind,留最后/单独立项)→ recon/llm/jwt(难);承载收尾(摘 MRO + ctx 承载 + 调用站切 ctx)与 `state` 真承载(rebind 坑)仍延后。

#### L3「真断透传」线 L3e 完成(2026-06-22,AuditInfra RT 簇对象化·执行体对象化第四刀·拆分方案,refactor `cc75c06`)
切法 A 第四刀,**主控派 Plan 测绘**评估 AuditInfra(`AuditInfraMixin` 13 方法,~345 行)对象化可行性。测绘核实后**推翻"深耦合底座"预判**:AuditInfra 实为 **5 个半独立簇**(session ledger / artifact registry / checkpoint / source hint / runtime action),整体切法 A 评 4/5 可行,但整体打有两代价:① `_ledger_run_id` 双向流 + setup 接力(coordinator 回读 dispatcher 字段);② 测绘**挖出既有缺陷**——`eval/replay.py` 仅 `set_notes_file` 隔离 notes,`run()` 连 `registry_root` 入参都没有(coordinator 硬编码 `None`)→ replay 把 session_ledger/artifact_registry/checkpoint 写进真实 `loot/`(对象化之前就有的洞)。**主控裁决取拆分方案 B**:只吃最闭合的 **RT(runtime action)簇**——3 个 `_runtime_*_action`(browser/proxy/execute_command)只依赖 `self.runtime`(稳定)+ `self._record_session_event`(兄弟注入),不碰任何 store 字段/run_id/bool/state;反向调用面最大(122 处)但形态全 `self.` 统一→委派壳零改;**RT 不持三个 store → 完全绕开 replay 隔离洞**,这刀不动 replay.py/coordinator,纯净低风险。**L3e 落地**:抽 stateless `RuntimeAuditedActions` 类(无 `__init__`、`vars()=={}` 证零自持),3 方法逐字搬(`self.runtime`→注入、`self._record_session_event`→注入 bound method);`AuditInfraMixin` 3 方法保原名原签名退委派壳(122 调用站零改);dispatcher `__init__` 持 `self._runtime_actions`;**MRO/调用站/coordinator/其余 10 个 AuditInfra 方法/三 store 字段/replay.py 全零改**。**门禁**:`tests/unit/agents` 523 passed(零回归)+ `test_replay_harness.py` 5 passed——主控独立坐实(亲跑 `test_ctf_audit_infra` 5 + replay 5)。**后续 L3f(预登记)**:先补 replay 三处落盘隔离洞(`run()`/coordinator 增 `registry_root` 透传 + tmp 重定向),再做 AuditInfra store 三簇(S/RA/CP)+ SH 簇切法 A 带三处产物对拍——中风险,牵动 coordinator/run() 签名,需主控先测绘+裁决。

#### L3「真断透传」线 L3f-1 完成(2026-06-22,补 replay 三处落盘隔离洞·拆卡甲,fix `615890c`)
L3e 测绘挖出既有缺陷:三个 audit store(session_ledger/artifact_registry/checkpoint)在 `audit_infra.py` `_setup_*` 硬编码 `Path("loot")/...` 默认根、setup 即 mkdir;`run()`/`coordinator.execute()`/`_bootstrap_dispatcher` 透传链已有 `ledger_root`/`checkpoint_root` 但**缺 `registry_root`**(coordinator `:709` 硬编码 None),`run_replay` 也没传任何 root → replay 把三处写进真实 `loot/`(对象化之前就有的洞)。**主控派 Plan 测绘后拆卡:甲(本卡,replay 隔离·低风险)先于乙(store 簇对象化·中风险),关注点正交且甲是乙做产物对拍的前提**。**L3f-1 落地**:`run()`/`execute()`/`_bootstrap_dispatcher` 各加 `registry_root`(kwonly·默认 `None`)+ `:709` 改透传;`run_replay` 在 `TemporaryDirectory` 块给三个 store root 全指向 tmp 子目录(不需 finally 复原——store root 是 per-call 入参非全局 module state,run 完即弃、tmp 退出即清);三个 `_setup_*` body 未动。**生产(非 replay)路径不传这些参 → 默认 None 走 `xxx_root or 默认`原逻辑,行为字节不变**;`CoordinatorDispatcherServices` Protocol 的 `_setup_*` 签名本已含 `registry_root`,无需改。新测 `test_replay_does_not_touch_real_loot_stores`(replay 前后真实 loot 三目录快照断言无新增)。**连带改动**:`test_ctf_coordinator.py` 的 `_FakeCoordinator.execute` 替身镜像新签名(否则新 kwarg 触发 TypeError fallback 误抛)。**门禁**:`test_replay_harness.py` 6 passed(5 fixture reproduced + 新隔离守护)+ `tests/unit/agents` 523 passed——主控亲跑 replay 6 passed 独立坐实。**为 L3f-2(乙·AuditInfra store 簇对象化带产物对拍)开闸**。

#### L3「真断透传」线 L3f-2 完成(2026-06-22,AuditInfra store 簇对象化·拆卡乙·执行体对象化收官最大一刀,refactor `8ecd999`)
甲(L3f-1)合入后派乙。AuditInfra 剩余 10 方法(session ledger / artifact registry / checkpoint / source hint / `_record_recovery_decision`)**一次抽全到单个 stateless `AuditStore`**(分簇会制造 `_record_session_event` sink 跨 class 注入、不划算)。切法 A 落地 + 盯死不变量:**三个 store 对象 + 三个 run_id + `_registered_local_source_hints_loaded` flag 全留 dispatcher `__init__`**(coordinator 直读 `_ledger_run_id`/Protocol 声明它 + flag;`_restore_context:1644/1661/1663` 直读 `_checkpoint_store`),`AuditStore` 零自持(`vars()=={}`);`_setup_*` 委派壳让独立方法**返回 `(run_id, store)` 写回 `self._x`**(artifact/checkpoint 经 `fallback_run_id=self._ledger_run_id` 保两步 fallback 接力链);写事件簇 per-call 传 store 引用+run_id+state + `record_session_event` bound method 注入;`_ingest` 的 `state is None`→`loaded` 守卫顺序逐字保留在壳、收尾置 `loaded=True`;落盘默认 `Path("loot")/...` 三处不变。**`_restore_context` 零改、coordinator 零改、MRO/调用站零改**(切法 A 核心收益:store 留 dispatcher 传引用 → 外部直读点不受影响)。**门禁**:`tests/unit/agents` 534 passed(零回归)+ DoD 四件套(audit_infra/artifact_registry/checkpoint_store/coordinator)81 passed + 整文件 replay 6 passed——主控独立坐实(亲跑 DoD 81 + 整文件 replay 6 + 隔离测试单跑绿)。**至此 AuditInfra 整体对象化完毕**(RT 簇 L3e + store 簇 L3f-2),dispatcher 现持 5 个独立 executor 对象(`_progress`/`_flag_observer`/`_note_store`/`_runtime_actions`/`_audit_store`)。
**⚠ 附带发现(卡 L3g)**:执行 agent 环境曾见 `test_replay_does_not_touch_real_loot_stores` 1 failed,经主控查证**与 L3f-2 无关**(整文件主控环境 6 passed、隔离测试单跑绿、534 全量零回归)——系 L3f-1 隔离补洞未闭合留下的**间歇旁路泄漏**(replay 链路某条 store 构造未拿 tmp root、退回默认 `Path("loot")`,异步/旁路间歇触发)。登记 L3g 收尾:测绘定位旁路点 + 补 root 透传或加 store 基址钩子(类比 `set_notes_file`)+ 隔离测试转稳定绿。非阻塞(主路径 fixture 全 reproduced)。

#### L3「真断透传」线 L3g 完成(2026-06-22,replay 隔离测试间歇红修复·根因翻转,fix `6204315`)
收 L3f-2 附带登记的 L3g。**主控派只读测绘坐实,翻转 L3f-1 遗留的根因假设**:间歇红**不来自 replay/生产旁路**——测绘用内存 monkeypatch(patch `SessionLedger.__init__`/`append_event`,不写盘)跑完整 `run_replay`,**0 次命中真实 loot**(三 root 全程透传、`_restore_context` 只读不构造)。**真凶在测试侧**:`tests/unit/agents/test_ctf_dispatcher.py` 里 ~26 个 `await dispatcher.run(...)` 不传 `ledger_root` → `AuditStore.build_session_ledger` fallback 到 `Path("loot")/session_ledgers` → 每跑一个往**真实 loot** 写一个 `ctf-<uuid>.jsonl`;隔离测试只在与这些 dispatcher 测试**同进程**跑时(全量/L3f-2 agent 环境)才把新文件算进 before/after 快照 delta → 间歇红。单跑 replay 模块永远绿(10/10 验证)。**L3g 落地(测试侧根治,生产零改)**:`audit_infra.py` 加进程级默认 loot 基址钩子 `set_default_loot_root`/`get_default_loot_root`(模块级默认仍 `Path("loot")`,**生产字节不变**),三个 `build_*` 的 `*_root` fallback 改读该默认值;`tests/unit/agents/conftest.py` 加 autouse fixture 每测试 `set_default_loot_root(tmp_path/"loot")`、teardown 还原 → 任何漏传 root 的构造落 tmp 不污染真实 loot,从根上防同构回归。**不碰 replay.py/coordinator.py/ctf_dispatcher.py**(已干净)。**门禁**:`tests/unit/agents` 534 passed(零回归)+ replay 整文件 6 passed(含隔离测试)+ 隔离测试连跑 5 次 delta 恒 0(修前间歇 +26)——主控独立坐实(全量 534 + replay 6 + 5×delta=0 亲跑)。**审计教训**:执行 agent 改完代码后陷入轮询空转、未提交未验证即歇手(回报仅"waiting for notification"),主控核 git 发现产出在工作树未提交 → 亲跑门禁 + 显式 pathspec 收尾提交,产出未丢。

#### L3「真断透传」线 L3h 完成(2026-06-22,JWT executor 对象化·执行体对象化第七刀·破冰,refactor `7c92ff4`)
AuditInfra 收官后转攻最后三块硬骨头(recon/llm/jwt)。**主控派只读测绘评三者对象化适配度**:JWT **5/5**(6 纯函数·零落盘/零 LLM/零 runtime IO·coordinator 零依赖·外部仅 1 处统一调用)> LLM 4/5(769 行 15 方法·LLM 入口需 eval 兜底·中)> Recon 3/5(818 行 11 方法·兄弟耦合面最宽 12+ 注入·`_phase_recon`/`_explore_agenda_items` 在 Protocol·中)。**裁决取 JWT 破冰**(等价 L3e 选 RT 簇:反向调用统一、不碰双向状态),定序 JWT→LLM→Recon。**L3h 落地**:`jwt_executor.py` 抽 stateless `JWTExecutor`(无 `__init__`,`vars()=={}`),6 方法逐字搬;`self.state`→**per-call 传值**(铁律:共享 CTFState 绝不入实例,replay/fork 换实例会持旧引用=非零行为,同 L3d `_notes_log` 坑)、`_recent_local_source_hint_secret_candidates`→**回调注入**;`JWTExecutorMixin` 6 方法退委派壳保原名原签名+`__module__` 锚定;`ctf_dispatcher.__init__` 持 `self._jwt_executor`。**MRO/coordinator/Protocol/jwt_contact_chain(唯一外部调用面)全零改**。**门禁**:`tests/unit/agents` 538 passed(基线 534+4 新 detached 单测,零回归)+ jwt 定向 9 passed——主控独立坐实(亲跑 9 + 全量 538 + `git show --stat` 仅 3 白名单文件)。纯函数簇无需 replay/eval。**后续**:L3i(LLM·中·体量大需 eval 兜底)→ L3j(Recon·中·兄弟耦合最宽+触 Protocol,留最后);二者都碰 `ctf_dispatcher.py`,与 L3h 及彼此须串行。dispatcher 现持 6 个独立 executor 对象(+`_jwt_executor`)。

#### L3「真断透传」线 L3i 完成(2026-06-22,LLM executor 对象化·执行体对象化第八刀·本系列最大一刀,refactor `d7f97ac`)
JWT 破冰后按定序切第二刀(中风险)。`LLMExecutorMixin`(769 行 15 方法,含 `_run_llm_driven_exploration` 真 LLM 入口)抽 stateless `LLMExecutor`。**切法 A + 新增 per-call 容器范式**:15 方法逐字搬入,`self.llm`/`self.state`/`runtime`/`collector_port`/`capability_registry` 全经**短命 `LLMExecContext` dataclass**(委派壳 `_llm_exec_context()` 每调现造、6 个兄弟 bound method 塞进回调字段)传入,executor 内改调 `ctx.xxx(...)`——**llm 句柄/共享 CTFState 绝不入实例**(replay/fork 换 state/llm 持旧引用=非零行为,同 L3d/L3h 坑);`LLMExecutor` 本身 `vars()=={}`,`LLMExecContext` 是 per-call 短命容器非自持状态。**15 方法全留委派壳**(外部入口 `_run_llm_driven_exploration` 必留 + 现有测试断言全 15 方法 `__module__` 锚定 + 纯内部从稳);2 个 `@staticmethod` 随迁。**关键不变量**:`strategy_registry.py:513/542/575` 的 `hasattr(dispatcher,'_run...')` 守卫运行时仍 True(壳留在 mixin)。**MRO/coordinator/Protocol/strategy_registry/ssti_executor 全零改**。**门禁(中风险)**:`tests/unit/agents` 542 passed(基线 538+4 新 detached,零回归)+ **replay 整文件 6 passed**(5 fixture 重放 LLM-driven exploration 路径全 reproduced=LLM 入口行为零变核心兜底)——主控独立坐实(亲跑 replay 6 + 全量 542 + `git show --stat` 仅 3 白名单)。**后续**:L3j(Recon·中·兄弟耦合最宽 12+ 注入 + `_phase_recon`/`_explore_agenda_items` 在 Protocol·留最后一刀);碰 `ctf_dispatcher.py` 须串行。dispatcher 现持 7 个独立 executor 对象(+`_llm_executor`)。

#### L3「真断透传」线 L3j 完成(2026-06-22,Recon executor 对象化·执行体对象化第九刀·最后一刀·L3 系列收官,refactor `54414a7`)
三硬骨头收官刀,三者最难(兄弟耦合最宽 + 触 Protocol)。`ReconExecutorMixin`(grep 原始版坐实**实为 9 方法**,测绘"11"多估)抽 stateless `ReconExecutor`(`vars()=={}`)+ per-call `ReconExecContext` dataclass 容器(承袭 L3i 范式)。9 方法逐字搬,`self.state`/`runtime`/`reasoning_layer` per-call 经 ctx 传(不入实例,同 L3d/L3h/L3i 坑)、**12 个兄弟 bound method**经 ctx 回调字段注入(`_runtime_browser_action`/`_is_legacy_browser_runtime_probe`/`_proxy_get_with_retry`/`_runtime_proxy_action`/`_scan_and_store`/`_store_note`/`_should_ignore_exploration_candidate`/`_extract_embedded_links`/`_emit`/`_fingerprint_framework`/`_classify_exploration_hint_strength`/`_form_action_url`)——执行 agent 自补测绘漏列的 `_scan_and_store`(首轮全量 1 个 AttributeError 自查自修)。9 方法全退委派壳。**Protocol 强约束达成**:`_phase_recon`/`_explore_agenda_items` 是 `CoordinatorDispatcherServices` 声明方法(coordinator.py:116/118),保壳确保 `isinstance(dispatcher, CoordinatorDispatcherServices)` 生产侧 conformance 仍 True。**MRO/coordinator/tui 零改**。**门禁(中风险)**:`tests/unit/agents` 545 passed(基线 542+3 新,零回归)+ replay 整文件 6 passed(recon 触 `_store_note` 落盘验隔离 + recon→auth 链行为不变)——主控独立坐实(亲跑 replay 6 + 全量 545 + grep 坐实原始 9 方法全对象化无遗漏)。

**🎉 L3 执行体对象化系列(L3a–L3j)全部收官**:dispatcher 从一个塞满 mixin 的 god-object 收敛为持 **8 个独立 stateless executor 对象**(`_progress`/`_flag_observer`/`_note_store`/`_runtime_actions`/`_audit_store`/`_jwt_executor`/`_llm_executor`/`_recon_executor`),每个 `vars()=={}`、状态全 per-call 传/注入。**切法 A 连验 9 刀(L3b–L3j)干净**:全程 MRO/调用站/coordinator/Protocol 零改,委派壳保签名+保 hasattr/conformance。**剩"承载收尾"独立主线**:8 个 executor 从 dispatcher 持实例 → ChainContext 承载(真 façade),会牵动 contract 签名、属高风险,需 live eval 回放兜底,单独立项。

## 6. 显式非目标(本轮不做)
- 不重构 foundation 依赖方向(已健康)。
- 不改 LLM provider / api_hub 路由逻辑。
- 不在 P1–P4 期间新增 CTF 解题能力(叶子能力补丁暂停,直到骨架就位)——这正是本 ADR 要纠正的旧惯性。

---

## 7. 契约选型(已确认 2026-06-17)
1. **`AgentSession` 落在 `pentestagent/session/`**(与 SessionStore/会话生命周期同居,且对非 interface 的 MCP 更中立)。
2. **P1 先改 CLI**(最小、独立)验证门面契约 + 顺手修 cpa-skip bug,再依次推 web / MCP / TUI。
3. **新建中立 `EventBus`**(放 `pentestagent/session/`);web 现有 EventBus、TUI notifier 回调、MCP `_emit` 逐一适配到这条中立总线。

---

## 8. 变更记录(总索引)

> **§8 为变更总索引;§5.2「进展日志」保留为详述,二者并存。**今后每完成一张卡 / 一个里程碑,
> 同时回写本表一行 + §5.2(或对应小节)详述。本表按时间/里程碑排序,只做总索引,
> 每行末列「详述位置」指向 §5.2 等对应条目。commit 短哈希仅取自详述已写明者,
> 无单一 commit 的多刀/跨刀里程碑填「见 §5.2」(详述含逐刀分解),不臆造哈希。

| 日期 | 卡 / 里程碑 | commit 短哈希 | 一句话摘要 | 详述位置 |
|---|---|---|---|---|
| 2026-06-17 | **P0** 定骨架 + 两契约(纸面) | `65de99a` | 本 ADR + 契约选型,零代码改动 | §5.2 |
| 2026-06-17 | **P1-a** EventBus + AgentSession 门面 | `d6c1712` | 中立 `EventBus`(I3)+ `AgentSession` 门面 + `RunResult`(I2),10 单测 | §5.2 |
| 2026-06-17 | **P1-b** CLI 走门面 + 修 cpa-skip bug | 见 §5.2 | CLI 三模式经 `AgentSession.create`,关闭 CLI 的 CPA M1–M6 静默跳过 bug | §5.2 |
| 2026-06-17 | **P1-web** 装配 + EventBus 统一 | `77a2618` · `c25d967` | web 修 CPA-skip + 私有 EventBus 收敛到中立总线 | §5.2 |
| 2026-06-17 | **P1-mcp** I3 收敛 | `6a0ca41` | MCP `_emit`/`_ui_hook` → 中立 EventBus(本就无 CPA bug) | §5.2 |
| 2026-06-17 | **P1-tui** I3 收敛 | `c25f747` | notifier 通道 bus 化(TUI 一行未动);本就经 `build_agent_components` | §5.2 |
| 2026-06-17 | **关节 A 全闭合 / P2** | 见 §5.2 | I2/I3 全线达成;crew 与单 agent 同源走 `notify()`;`RunResult` 结果契约 | §5.2 |
| 2026-06-17 | **P1-b/P2** 组合根下沉 | 见 §5.2 | `session/initializer.py` 成中立组合根,`interface/initializer.py` 退为兼容 re-export | §5.1 / §5.2 |
| 2026-06-17 | **P3a** `_execute_chain` → handler map | 见 §5.2 | if/elif 分发收缩为 handler map 路由 + 统一 LLM fallback,加防回流守卫 | §5.2 / §5「P3 风险发现」 |
| 2026-06-17 → 06-20 | **P4** 8 chain mixin 迁出 | 见 §5.2 | dispatcher 上 chain 方法物理迁入 `chains/{web,sqli,xss,ssti,upload,jwt,misc,injection,file_read}.py` | §5.2 |
| 2026-06-20 | **P5(god-object)** dispatcher 23 刀拆分 | 见 §5.2 | `ctf_dispatcher.py` 9733→1758(**−82%**),载荷分 23 刀外迁到 ~17 个 `XxxMixin`,每刀零回归 | §5.2 + §5「P5 收敛宣告」 |
| 2026-06-20 | **P5(命名/文档线)/ 卡 B** | `67ba49e` | cpa_modules m1–m6 职责对照表 + capability registry 收尾(修 M5 悬空导出) | §5「P5(命名/文档线)收尾」 |
| 2026-06-20 | **P3b 第②刀** state 通道收口 | 见 §5.2 | `strategy_registry.py` 内 `ctx.dispatcher.state` 透传 5→0(可证死代码) | §5.2「P3b 第二刀」 |
| 2026-06-20 | **P3b 第③刀** 零散状态查询下沉 | 见 §5.2 | 新增 `ingress_handoff`/`challenge_context` 显式字段,经 dispatcher 的状态查询清零 | §5.2「P3b 第三刀」 |
| 2026-06-20 | **P3b 主体刀** execute lambda 透传破除 | 见 §5.2 | 22 处 `ctx.dispatcher._run_*/_attempt_*` → `ctx.services.*`(`services=self` 过渡) | §5.2「P3b 主体刀」 |
| 2026-06-20 | **P3b 第④刀 / 卡 A**(收口) | `0308774`(docs `057f5c2`) | 摘 `dispatcher: Any` + 删 4 死字段,`ChainContext` 字段 13→8;P3b 收口 | §5.2「P3b 第④刀」 |
| 2026-06-20 | **卡 C** eval / 回放 harness 扩充 | `808482c` | 为缺口链路新增 record fixture + 可重放 replay 测试 | backlog 卡 C |
| 2026-06-20 | **卡 E** backup_node_app:zip 验收链转绿 | `ed8cdb4` | `artifact_forensics.py` 补 `import json`,benchmark 转绿(14/14) | §5.2「卡 E 收口」 |
| 2026-06-20 | **卡 D** M4 `mask_ips` 语义核查 | `826bd70` | DataProtector `mask_ips` 复核 + 结论落对照表 §4 | backlog 卡 D / 对照表 §4 |
| 2026-06-21 | **卡 F** M6 `apply_turbo` 死代码删除 | `837f0d2` | 删零调用 `apply_turbo`,保留 `_wrap_tool` registry + deadcode 守卫 | §5.2「P5 命名/文档线尾巴」 |
| 2026-06-21 | **卡 G** initializer 门控统一 | `b52cb0c` | M1/M3/M4/M6 走 `is_mN_enabled()`;M2/M5 刻意保留直读 env(避免自锁) | §5.2「P5 命名/文档线尾巴」 |
| 2026-06-21 | **卡 H** easy_tornado 观测命名复核 | `399f262` | 根因文档 §1.1/§1.2 补复核结论(纯 docs,未触生产) | §5.2「P5 命名/文档线尾巴」 |
| 2026-06-21 | **P3b L1 / 卡 L1**(P3b 真收口) | `b73b6bf` | `services: Any` → `StrategyServices` Protocol(`@runtime_checkable`,20 必调成员 + 3 守卫留外);live 回放零差 | §5.2「P3b L1」 |
| 2026-06-21 | **P3b L2a / 卡 L2a**(关节 B 延伸·破冰刀) | `4960eaf` | coordinator `dispatcher: Any` → `CoordinatorDispatcherServices` Protocol(`@runtime_checkable`,39 成员=22 方法+17 属性,`strategy_memory` 纳入标 `Any\|None`;7 守卫留外;裁决保留 4 处 `Any`);23 处形参收窄、调用体零改;live 回放零差 | §5.2「P3b L2a」 |
| 2026-06-21 | **P3b L2b / 卡 L2b**(关节 B 延伸·收口刀) | `6f43a87` | 收窄 RunContext 的 `dispatcher` 形参/property 到 Protocol,闭合 L2a 在包装器的 `Any` 缺口;纳入逃生口 `_run_solve_loop`(Protocol 39→40),RunContext 靠 `__getattr__` 结构化合规、不显式继承;余 2 处模块级 helper `Any` 裁决保留;调用体零改、live 回放零差 | §5.2「P3b L2b」 |
| 2026-06-21 | **P3b L2c / 卡 L2c**(关节 B 延伸·生产侧闭环) | `b200123` | 对称 L1,为 `CoordinatorDispatcherServices` 补生产侧 conformance 断言(`isinstance(real_dispatcher, ...)`,CI 强制——mypy 未安装/不进 CI,pytest 是唯一门);test-only 零生产改动;agents 490 passed、live 回放零差 | §5.2「P3b L2c」 |
| 2026-06-21 | **卡 N1**(S 扫描·死代码/bug) | `6afd433` | 删 llm/utils.py 5 死函数 + count_tokens/truncate_to_tokens,修 count_tokens 恒等分支;parse_llm_json 保留 | §5.2「N1–N4 批次」 |
| 2026-06-21 | **卡 N2**(S 扫描·死代码) | `6e2126d` | 删 m6_turbo 5 个零调用顶层便捷包装,保留 LazyLoader/MemoryOptimizer 类 | §5.2「N1–N4 批次」 |
| 2026-06-21 | **卡 N3**(S 扫描·bug) | `27b9092` | 修 base_playbook 非 dataclass 误用 `field()` 致 phases 潜伏 bug + 守卫测试 | §5.2「N1–N4 批次」 |
| 2026-06-21 | **卡 N4**(S 扫描·死代码) | `8e77449` | 删 knowledge/retrospective.py 死代码 `_safe_list` | §5.2「N1–N4 批次」 |
| 2026-06-21 | **卡 C5**(S 扫描·判定保留) | `4884979` | token_tracker.set_data_file 判定测试专用 API,保留+docstring 标注 | §5.2「N1–N4 批次」 |
| 2026-06-21 | **卡 C6**(S 扫描·判定保留) | `b88bb81` | m5 get_vote_status 判定预留契约接口,保留+补 4 契约测试 | §5.2「N1–N4 批次」 |
| 2026-06-21 | **卡 S1+S2**(S 扫描②·真 bug) | `6de4d1d` | pheromone_router heapq 同分崩溃(tie-breaker)+ is_active 阈值失效(吃 self._threshold) | §5.2「S 扫描②批次」 |
| 2026-06-21 | **卡 S3**(S 扫描②·死代码) | `0e5ea9f` | 删 interface/utils.py 4 展示函数 + 级联 truncate_output | §5.2「S 扫描②批次」 |
| 2026-06-21 | **卡 S4**(S 扫描②·docstring) | `0c7f0cb` | cost_tracker.get_recent_logs docstring 改"从旧到新"对齐实现 | §5.2「S 扫描②批次」 |
| 2026-06-21 | **卡 S5**(S 扫描②·真 bug) | `9704e20` ⚠ | indexer 大写 .JSON 误走 YAML 修复(该 commit message 因并发竞争错挂 S4 标题,真实映射见 §5.2/backlog) | §5.2「S 扫描②批次」 |
| 2026-06-22 | **L3a / 卡 L3a**(L3 真断透传·破冰刀) | `3bd372e` | RunContext 加空白名单字段分流机制(`_OWN_FIELDS=frozenset()`,object.__* 分流,内部名短路);白名单空→等价纯透明代理零承载,为 L3b 承载 state 开闸;500 passed、live 回放零差 | §5.2「L3a」 |
| 2026-06-22 | **L3b / 卡 L3b**(L3 真断透传·执行体对象化第一刀·裁决换序) | `35d7649` | 测绘裁定 state 承载因 resume rebind 坑延后,改在最闭合的 ProgressTracker 验证对象化范式;切法 A:抽独立 `ProgressTracker` 类(零 eager 持 state)+ mixin 退化委派壳,不动 MRO/调用站;425 passed 零回归、新测 6 passed、独立坐实通过 | §5.2「L3b」 |
| 2026-06-22 | **L3c / 卡 L3c**(L3 真断透传·执行体对象化第二刀) | `563d525` | 测绘比选 3 候选取 flag_observer(唯一零反向依赖底座);切法 A:抽 `FlagObserver` 类(state/context per-call 传、verifier+4 兄弟方法 per-call 注入)+ mixin 退化委派壳(35 调用站零改),不动 MRO;515 passed 零回归 + replay 5 passed live 兜底;过程撞 529 过载经用户 `!` 自助落地 | §5.2「L3c」 |
| 2026-06-22 | **L3d / 卡 L3d**(L3 真断透传·执行体对象化第三刀) | `2de4d1f` | note_store 对象化;切法 A:抽 stateless `NoteStore` 类,唯一坑 `_notes_log` 留 dispatcher per-call 传 list 引用(从不自持,result.notes 仍读同一 list);落盘非风险(notes_tool 单例+replay set_notes_file 隔离)、40 反向调用经委派壳零改;519 passed 零回归 + replay 5 passed(主控亲跑印证) | §5.2「L3d」 |
| 2026-06-22 | **L3e / 卡 L3e**(L3 真断透传·执行体对象化第四刀·拆分方案) | `cc75c06` | AuditInfra(13 方法/5 簇)测绘后只取最闭合的 RT 簇;切法 A:抽 stateless `RuntimeAuditedActions`(3 个 `_runtime_*_action`,runtime+session-event sink per-call 注入,`vars()=={}`)+ mixin 退委派壳(122 调用站零改);绕开 replay 隔离洞(RT 不持 store);523 passed + replay 5 passed(主控独立坐实);store 三簇+隔离洞留 L3f | §5.2「L3e」 |
| 2026-06-22 | **L3f-1 / 卡 L3f-1**(replay 落盘隔离补洞·拆卡甲) | `615890c` | L3e 测绘挖出既有缺陷:replay 把 session_ledger/artifact_registry/checkpoint 写真实 loot;补 `run()`/`execute()`/`_bootstrap` 的 `registry_root`(kwonly·None 默认)透传 + `run_replay` 三 root 指向 tmp;生产路径零行为(默认 None);replay 6 passed(含新隔离守护)+ agents 523 passed(主控亲跑印证);为 L3f-2 产物对拍开闸 | §5.2「L3f-1」 |
| 2026-06-22 | **L3f-2 / 卡 L3f-2**(AuditInfra store 簇对象化·拆卡乙·收官) | `8ecd999` | AuditInfra 剩余 10 方法一次抽全到 stateless `AuditStore`(`vars()=={}`);三 store/三 run_id/flag 留 dispatcher、`_setup_*` 壳回写 self、写事件簇 per-call 传引用注入;`_restore_context`/coordinator/MRO/调用站全零改;534 passed + DoD 四件套 81 + 整文件 replay 6(主控独立坐实);AuditInfra 整体对象化完毕。附:间歇旁路泄漏归 L3g(非本刀) | §5.2「L3f-2」 |
| 2026-06-22 | **L3g / 卡 L3g**(replay 隔离测试间歇红修复·根因翻转) | `6204315` | 测绘翻转 L3f-1 根因假设:泄漏非 replay/生产旁路,而是 `test_ctf_dispatcher.py` ~26 个 `dispatcher.run()` 漏传 `ledger_root` 往真实 loot 写 `ctf-*.jsonl`;测试侧根治=audit_infra 加 `set_default_loot_root` 钩子(默认仍 `Path("loot")` 生产零改)+ `tests/unit/agents/conftest.py` autouse fixture 指 tmp;534 passed + replay 6 + 5×delta=0(主控独立坐实) | §5.2「L3g」 |
| 2026-06-22 | **L3h / 卡 L3h**(JWT executor 对象化·破冰) | `7c92ff4` | 测绘评 recon/llm/jwt 适配度(JWT 5/5 最闭合)后破冰:`JWTExecutorMixin` 6 纯函数抽 stateless `JWTExecutor`(`vars()=={}`),state per-call 传、source-hint 回调注入,退委派壳;MRO/coordinator/Protocol/jwt_contact_chain 零改;538 passed(+4 detached)+ jwt 定向 9(主控独立坐实);dispatcher 持 6 个 executor。定序 JWT→LLM→Recon | §5.2「L3h」 |
| 2026-06-22 | **L3i / 卡 L3i**(LLM executor 对象化·本系列最大一刀·中风险) | `d7f97ac` | `LLMExecutorMixin` 769 行 15 方法抽 stateless `LLMExecutor`(`vars()=={}`),per-call `LLMExecContext` 传 llm/state/runtime + 6 兄弟回调注入,15 委派壳保签名+保 `hasattr` 守卫;MRO/coordinator/strategy_registry/ssti_executor 零改;542 passed(+4 detached)+ replay 整文件 6(重放 LLM 入口零变·主控独立坐实);dispatcher 持 7 个 executor | §5.2「L3i」 |
| 2026-06-22 | **L3j / 卡 L3j**(Recon executor 对象化·最后一刀·L3 系列收官) | `54414a7` | `ReconExecutorMixin`(实 9 方法)抽 stateless `ReconExecutor`(`vars()=={}`)+ per-call `ReconExecContext`,12 兄弟方法回调注入,9 委派壳保 Protocol conformance(`_phase_recon`/`_explore_agenda_items`);MRO/coordinator/tui 零改;545 passed(+3)+ replay 6(主控独立坐实)。**L3a–L3j 收官**:dispatcher 持 8 个独立 stateless executor,切法 A 连验 9 刀干净。剩承载收尾(高风险·单立项) | §5.2「L3j」 |
