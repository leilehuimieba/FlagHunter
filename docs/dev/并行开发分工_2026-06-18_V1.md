# 并行开发分工（Multi-Agent Parallel Dev）V1

- 日期：2026-06-18
- 定位：多 agent（本会话 + 多个 Codex 会话 + 用户本人）**同仓并行**的单一真相源。开新会话前先读本文，按此认领文件、定契约、走合并。
- 心法（承 [[项目工程治理流程_V1]]）：**一个文件一个主人 · 每会话独立 worktree · 契约先定 · 一个集成人**。

---

## 0. 三条铁律（违反必冲突）

1. **一个文件一个主人**：下表"可改文件"互不相交。不在你名下的文件**只读**，需要改就找集成人协调，别直接动。
2. **每会话独立 git worktree**（不是同一工作目录！）：
   ```bash
   git worktree add ../fh-<stream> -b feat/<stream> main
   ```
   各 worktree 独立工作树 + 独立分支，互不污染。完工推分支，由集成人合并。
3. **契约先定**：跨流要用到的接口（函数签名 / 返回结构）在下表"契约"列冻结；冻结后不许单方面改，要改先在本文改契约并通知。

---

## 1. 热点文件（共享，禁止并行写）

这些文件多流都可能想动，**只能由集成人或其指定主人串行改**：

- `flaghunter/agents/pa_agent/ctf_dispatcher.py`（巨型中枢，用户 P4 在动）
- `flaghunter/agents/pa_agent/strategy_registry.py`、`hypothesis_engine.py`、`chains/web.py`
- `tests/unit/agents/test_strategy_reachability.py`（I5 守护）
- `docs/README.md`、`docs/dev/基准_*`、本文

---

## 2. 分工表（当前波次）

| 流 | 负责人 | 目标 | 可改文件（owned） | 禁改（off-limits） | 契约 |
|---|---|---|---|---|---|
| **S0 集成 + 次步A/B/C** | 本会话（Claude） | runtime-parity / eval harness / ssti 退役确认 | `flaghunter/runtime/*`、新 `flaghunter/eval/*`、`tests/unit/runtime/*`、`tests/eval/*` + 热点文件（§1） | 其余 | 见各 commit |
| **S1 混合检索** | Codex#1 | M1：BM25+向量 RRF 融合 | `flaghunter/knowledge/rag.py`、`flaghunter/knowledge/indexer.py`、对应 `tests/` | 其它全部 | `RAGEngine.search(query)->list` 返回结构不变，仅内部融合 |
| **S2 记忆压缩** | Codex#2 | M2/M3：按来源分压 + 摘要成结构化 task-state | `flaghunter/llm/memory.py`、对应 `tests/` | 其它全部 | `ConversationMemory` 公共方法签名不变 |
| **S3 工具目录懒加载** | Codex#3 | T2：工具/MCP 两行 index + `load_tool_schema(name)` 选中才注全 schema | `flaghunter/tools/loader.py`、对应 `tests/` | `tools/executor.py`、其它全部 | `loader` 对外注册接口不变 |
| **P4/P5** | 用户 | chains 拆包收尾 / cpa_modules | `chains/xss.py`、`ctf_dispatcher.py`、`hypothesis_engine.py`、用户的 P4 测试 | runtime/eval/knowledge/llm/tools/loader | — |

> 为什么是 **3 个 Codex + 集成人 = 4 流**：本仓有 `ctf_dispatcher.py`/`strategy_registry.py` 等强中枢文件，>4 流并行时合并冲突的损耗会盖过并行收益。要再加只加**完全独立的新包**（如新 `flaghunter/observability/` 的 E10 span），不要再塞进已有热点文件。

---

## 3. 每流的完成定义（DoD，承治理文档）

1. 在自己 worktree 的分支上，**先写测试**（红）→ 最小实现（绿）。
2. 只跑**自己 owned 范围**的测试即可快速迭代；**不要**改 §1 热点文件让自己的测试过。
3. 完工：推分支（`git push origin feat/<stream>`），在交接里写清"改了哪些文件、新增哪些测试、契约有无变化"。
4. 集成人逐分支合并到 main，每次合并后跑**全量** `pytest tests/unit`，绿了才推。

---

## 4. 合并顺序（集成人）

知识/记忆/工具流（S1/S2/S3）与 agent/runtime/eval 流（S0）**文件不相交 → 合并零冲突**。顺序：先合独立度最高的 S1/S2/S3，再合 S0，最后协调用户 P4/P5（它动热点文件，放最后单独对齐）。

---

## 5. 给 Codex 会话的任务书（可直接 paste）

> 每个 Codex 会话开局先 paste 对应一段。已假定它在自己的 worktree/分支里。

### S1（Codex#1）— M1 混合检索 RRF
```
你在 FlagHunter 仓库的独立 worktree/分支上工作。只允许改：flaghunter/knowledge/rag.py、
flaghunter/knowledge/indexer.py 及对应 tests/。其它文件一律只读，绝不修改（尤其
ctf_dispatcher.py / strategy_registry.py / docs/）。
任务：给当前 FAISS-only 的 RAGEngine.search 加 BM25 索引并用 RRF（1/(k+rank), k≈60）融合
向量与 BM25 结果。理由：向量对 CVE/版本/端口/参数名等精确 token 系统性漏召，CTF 命门正是
精确 token。约束：search(query) 的对外返回结构不变，只改内部召回。先写测试（构造一个精确
token 用例证明 BM25 召回它而纯向量漏召），再实现。BM25 依赖门控在 [rag] extra。完工跑
自己范围的 tests，推分支，写交接说明。
```

### S2（Codex#2）— M2/M3 记忆压缩
```
你在 FlagHunter 仓库的独立 worktree/分支上工作。只允许改：flaghunter/llm/memory.py 及对应
tests/。其它文件只读。
任务：(M2) 按观测来源分型压缩——HTTP body/headers、stdout、stderr、错误栈各自 schema，
而非统一截断；(M3) 把摘要从散文改为结构化 task-state（confirmed_facts / open_hypotheses /
rejected_payloads / pending），保留原文指针。约束：ConversationMemory 公共方法签名不变。
先写测试（同样输入下，结构化摘要保留关键事实、丢弃噪声 banner），再实现。完工跑自己范围
tests，推分支，写交接。
```

### S3（Codex#3）— T2 工具目录懒加载
```
你在 FlagHunter 仓库的独立 worktree/分支上工作。只允许改：flaghunter/tools/loader.py 及对应
tests/。禁改 tools/executor.py 与其它文件。
任务：把工具/MCP 动作改为先只注两行 index（名+一行描述），新增 load_tool_schema(name) 选中
才注入完整 schema，削减 prompt 膨胀与错选工具。约束：loader 对外注册接口不变。先写测试
（未选中时上下文只含 stub，调用 load_tool_schema 后才有全 schema），再实现。完工跑自己范围
tests，推分支，写交接。
```

---

## 6. 维护

波次结束（合并完成）后归档本表或开 V2。新增并行流先在 §2 登记 owned/off-limits/契约，再开会话。
